# VibePolaris 设计 QA（最新：首页 R4.2 跨页流星）

## 首页 R4.2 跨路由流星

### 动效结构

- 单击已有详情页的概念后，先复用 R4.1 的星位交换：概念星在 470ms 内进入中心，分类中心星回到原概念槽位。
- 概念星不在中心停住，继续进入 760ms 的跨路由飞行；280ms 时预取完成的 Next.js 路由开始切换，星体在根布局中保持为同一个飞行层，不随页面卸载。
- 飞行轨迹使用二次曲线采样的三个路径节点，尾迹继续使用品牌 `meteor-trail-mask.png`，详情页真实星体作为最终落点。新页面出现后会读取实际落点位置，避免响应式布局带来的估算偏差。
- 页面只在星体飞行时短暂降低主体透明度并下沉 9px，导航保持稳定；飞行结束后移除覆盖层，详情页标题左侧只保留一颗 54px 真实北极星。

### QA 结果

- 桌面应用内浏览器完成“前端 → CSS”单击跳转，依次检查换位、跨页中段、落地和静止状态；最终 URL 为 `/terms/css`，飞行层数量为 0，目标星体数量为 1、不透明度为 1。
- 390×844 iframe 完成同一链路；落地后 `scrollY=0`，飞行层数量为 0，标题与 42px 星体正常换行排布，未见横向溢出。
- 组件、状态、响应式、表单、API、数据库、认证及原有 AI / 技术栈 / 产品详情入口同步复用这套动效；没有详情页的概念仍停留在中心聚焦，不伪造空详情页。
- CSS 新增为第 21 条结构化术语，生产构建静态生成 30 个页面。ESLint、TypeScript、13 项 Node 测试和生产构建全部通过。
- `prefers-reduced-motion` 的运行分支会跳过星位和跨页飞行，直接调用路由；本轮由代码断言与全局减少动态样式覆盖，应用内浏览器不提供系统媒体偏好切换入口。

未发现仍需处理的 P0、P1 或 P2 问题。

final result: passed

---

## 首页 R4.1 星位交换动效

### 实现与证据

- 概念点击：两颗真实北极星蒙版沿浅弧线交换位置，中心与来源文字先淡出，210ms 时更新语义内容，470ms 完成并移除飞行图层。
- 分类切换：旧星点向中心收拢淡出，150ms 后替换数据，新星点按 24ms 错峰从内向安全槽位散开。
- 静止状态：只保留一颗背景星每 7.6s 短暂闪烁，中心星每 8.4s 轻微变亮；不做持续漂浮或全场呼吸。
- 逐帧样本：`docs/design/qa/homepage-motion-swap-mid.png`、`homepage-motion-focus-end.png`、`homepage-motion-domain-end.png`。

### QA 结果

- 首版共享视图过渡会拉伸整块文字和焦点轮廓，并在连续交互时记录 `InvalidStateError`；已改为只动画两颗星体的独立飞行图层，不再缩放文字截图。
- 应用内浏览器实测概念聚焦、返回中心、Escape、分类聚散和 45ms 连续切换；最终仅保留 1 个中心星节点、飞行图层为 0，控制台日志为空。
- 390×844 真实 iframe 视口完成概念换位，中心链接、星体数量和排布正常。
- `prefers-reduced-motion` 下不创建飞行图层，分类和焦点状态直接更新。

final result: passed

---

## 首页 R4 极简技术星图

### 对照证据

- 视觉真值：`C:\Users\chuansgu\.codex\generated_images\01a04cd2-3ffd-71a3-9eca-5d7f98f45830\exec-a9bfb52e-e607-4d4c-8e19-8ac8d2918652.png`；项目内副本 `docs/design/qa/homepage-star-map-reference.png`。
- 视觉真值像素：1487×1058；生成目标画布为 1440×1024。
- 桌面实现：`docs/design/qa/homepage-star-map-implementation-1280x720.png`；应用内浏览器 CSS 视口与截图像素均为 1280×720，浅色 `moss`、AI · Agent 默认中心态。
- 同屏对照：`docs/design/qa/homepage-star-map-comparison.png`。两侧各放入 640×455 等大容器，使用 contain 等比缩放、不拉伸；由于应用内浏览器桌面视口固定为 1280×720，本轮按同一内容状态比较构图与层级，不做逐像素位置断言。
- 聚焦状态：`docs/design/qa/homepage-star-map-context-focus.png`；上下文进入中心，AI · Agent 回到周边星点。
- 分类状态：`docs/design/qa/homepage-star-map-backend.png`；后端成为唯一 `aria-pressed=true` 的分类，概念集合同步切换。
- 暗色状态：`docs/design/qa/homepage-star-map-dark.png`；1280×720。
- 移动端：`docs/design/qa/homepage-star-map-mobile-screen.png`；真实 390×844 iframe CSS 视口。应用内浏览器截图按 1.5 显示缩放得到 261×563 区域，随后归一化为 390×844；iframe 实测 `clientWidth=390`、`scrollWidth=390`、`clientHeight=844`、`scrollHeight=844`。

### Findings

未发现仍需处理的 P0、P1 或 P2。实现保留了参考图的横向技术分类、中心主题、无连线漂浮概念、暖骨白 / 苔藓绿 / 黄绿色北极星和大留白，同时把参考图右侧被裁切的概念收回安全区，保证真实页面无横向溢出。

### 五项保真检查

- 字体与层级：沿用项目系统中文字体；导航、分类、中心主题、概念四级字号清晰。中心标题在桌面 48–78px、移动端 35–48px，未出现截断或异常换行。
- 间距与布局：桌面 1280×720 的 `scrollWidth/scrollHeight` 与视口一致；390×844 也无横向或纵向溢出。分类数量继续由数据数组驱动，移动端使用横向滚动而非压缩全部标签。
- 色彩与令牌：只消费既有 `--bg`、`--ink`、`--muted`、`--accent`、`--brand-star`；浅色和暗色均保持足够对比，不引入蓝紫色、渐变、阴影或卡片表面。
- 图像与图标：品牌轨迹和所有星体继续使用项目真实 PNG 蒙版资产；没有用 emoji、字符星号、内联 SVG 或 CSS 图形替代。搜索与主题图标沿用既有站点组件。
- 文案与内容：首屏只保留品牌、技术分类、中心主题与概念名；旧 Hero、领域说明、列表解释、最近更新和 CTA 全部移除。

### 交互与无障碍

- 浏览器实际测试了分类切换、概念聚焦、中心术语详情链接、Escape 返回、搜索提交、明暗主题切换和移动端概念聚焦。
- 分类使用原生按钮与 `aria-pressed`，概念使用原生按钮，中心概念使用真实链接；状态变化通过 `aria-live` 播报。
- 桌面与移动端关键点击目标不小于 44px；键盘焦点有可见样式。
- `prefers-reduced-motion` 由样式回退与自动化断言覆盖；本轮应用内浏览器未暴露系统级动效偏好切换入口。
- 浏览器控制台日志为空。

### 比较历史

1. 初次实现为保证星图最小高度使用 610px，1280×720 下页面 `scrollHeight=854`，属于 P2 首屏比例漂移。将分类轨高度改为视口 clamp，并让星图高度取剩余视口空间；复验后 `scrollWidth=1280`、`scrollHeight=720`。
2. 概念按钮点击后原节点卸载，焦点回到 body，局部 `onKeyDown` 无法接收 Escape，属于 P2 键盘返回失效。改为窗口级 Escape 监听，保留组件卸载清理；复验中心态可恢复。
3. 移动端使用 390×844 iframe 实际渲染，复验分类横向浏览、星点不重叠、聚焦后仍为 `scrollWidth=390`、`scrollHeight=844`。

### Follow-up Polish

- P3：后续接入真实内容关系数据时，可用关联权重驱动星点距离与字号；当前使用固定的七个安全槽位，优先保证低噪与稳定排布。

final result: passed

---

## 历史：VibePolaris 首页 R3 设计 QA

## 对照范围

- 参考：苔藓编辑首页方案图，以及已选定的流星 / 北极星标识几何。
- 实装：`prototype/index.html`，1536×1024 默认浅色 `moss` 状态。
- 响应式：1536×1024、900×900、390×844。

## 结果

通过。当前实装保留参考方案的暖白纸张感、森林绿文字、黄绿色星体、发丝分隔线和技术领域横向编辑布局；按已确认反馈，把参考图中的“从技术领域开始”和大搜索框改成 VibePolaris 品牌首屏，导航搜索保留。流星图标使用真实透明资产并保持轨迹连续，星体比早期版本更大且与轨迹分色。

## 检查记录

| 优先级 | 检查项 | 结果 |
| --- | --- | --- |
| P0 | 页面可加载、脚本无语法错误 | 通过 |
| P0 | 领域切换始终只有一个展开内容 | 通过 |
| P0 | 390px 无横向溢出 | 通过 |
| P1 | 默认苔藓色与参考视觉一致 | 通过 |
| P1 | Logo 轨迹 / 星体资产、首入与 hover 动效 | 通过 |
| P1 | 明暗模式持久化、配色安全回退 | 通过 |
| P1 | 搜索查询页返回正确术语 | 通过 |
| P2 | 900px 双列与 390px 单列重排 | 通过 |
| P2 | `prefers-reduced-motion` 静态回退 | 通过（样式与自动化检查） |

未发现需要阻断交付的 P0、P1 或 P2 视觉问题。全页截图在应用内浏览器的拼接模式下偶发重复画面，DOM 节点数与普通视口截图均确认页面本身没有重复内容。

## BUG-21BCCBAD Logo 修复复验

### 证据与归一化

- 视觉问题来源：`docs/design/qa/logo-before-feedback.png`，112×81 像素，浅色 `moss` 静止状态。
- 修复后全视图：`docs/design/qa/logo-after-full.png`，1280×720 像素，对应 1280×720 CSS 视口。
- 修复后聚焦区域：`docs/design/qa/logo-after-focus.png`，190×165 像素，浅色 `moss` 静止状态。
- 两张聚焦图裁剪范围不同，因此不做逐像素重合判断；对照目标是用户明确指出的尺寸、终点对齐、星体层级和动画流畅度。

### 比较历史

**第一次反馈，结果 blocked。** P1：真实可视面积受透明素材边缘影响，首页标识虽然占位 74×64，但轨迹和星体明显偏小；星体中心落在轨迹终点左侧，视觉断开；轨迹使用 `clip-path` 逐帧揭示并强制同步布局重播，在蒙版图层上出现卡顿。

**修复。** 首页标识占位调整为 118×104，星体图层为 48×48；导航和页脚同步按比例增大。星体蒙版按透明边缘比例放大，中心重新落到轨迹终点；星体颜色从 `#A9C735` 提升到 `#BDD32F`。动画改为只使用 `translate3d`、`scale` 和 `opacity`，移除 `clip-path` 与强制读取 `offsetWidth`。

**第二次对照，当时结果 passed，后被真实观感反馈推翻。** 修复后轨迹可视宽高约为修复前的 1.7 倍，星体约为 3 倍；静止画面、1280px 与 390px 布局和控制台检查均通过。但该轮只验证了合成属性与少量静态状态，没有拆开中间运动帧，漏掉了 44% 与 74% 关键帧处的分段减速。

### 五项表面检查

- 字体与层级：wordmark 字体、字重和行高未受 Logo 放大影响；移动端 wordmark 缩至 40–52px，保持同一行。
- 间距与对齐：首页 Logo 与 wordmark 间距增至 38px，星体越界部分保留约 16px 视觉安全距；移动端 lockup 总宽 312px，未溢出 375px 内容区。
- 色彩：继续使用苔藓绿轨迹，星体提高黄绿色亮度但不引入蓝紫色或渐变。
- 资产质量：继续使用两张透明 PNG 蒙版资产；没有用 CSS 图形、字符或手绘 SVG 替代。
- 文案：无变化。

### 交互与无障碍

- 首入与悬停重播均使用合成属性；时长缩短为轨迹 520ms、星体 640ms。
- `prefers-reduced-motion: reduce` 下保持静态完整 Logo。
- 1280×720 桌面视口、390×844 移动视口与浏览器控制台已复验。

final result: passed

## BUG-21E057BE 首页降噪复验

### 视觉证据与状态归一化

- 用户标注源视觉：`docs/design/qa/homepage-density-user-annotation.png`，1923×1335 像素，红框指定需移除的导航副名、Hero 说明、领域说明、编号和长入口文案。
- 改动前浏览器源截图：`docs/design/qa/homepage-density-before.png`，1340×1218 像素，对应 1355×945 CSS 视口、密度已由浏览器归一化为 1×截图。
- 改动后浏览器实现：`docs/design/qa/homepage-density-after.png`，1340×1210 像素，同一 1355×945 CSS 视口、浅色 `moss`、AI · Agent 展开状态。
- 同屏全视图对照：`docs/design/qa/homepage-density-comparison.png`，2712×1274 像素；左侧为改动前，右侧为改动后。
- 额外响应式证据：`docs/design/qa/homepage-density-desktop-1536.png`，1520×1210 像素，对应 1536×960 CSS 视口；`docs/design/qa/homepage-density-mobile.png`，374×2307 像素，对应 390×844 CSS 视口。

### 比较历史

**用户标注时，结果 blocked。** P2：品牌定位在导航和 Hero 重复，领域上方辅助说明、“精选 01”类编号以及底部长 CTA 使页面扫读密度偏高。

**修复。** 移除上述重复文案，保留品牌主名、领域标题、短描述和主内容；五个领域入口视觉上仅显示箭头，同时保留 44×44px 点击区和完整 `aria-label`。

**修复后对照，结果 passed。** 同视口对照中，Hero 只剩单一品牌锁定，领域标题直接成为卡片首信息，入口箭头统一落在右下角。桌面 1536px、平板 900px 与手机 390px 的 `scrollWidth` 均不超过视口可用宽度；浏览器控制台无警告或错误。

### 五项表面与交互检查

- 字体与层级：字体家族、字重、字号与行高不变；删除信息后主标题与领域标题层级更明确。
- 间距与布局：Hero 高度保持 220px，仅改为单一锁定垂直居中；领域工具条缩至 64px，“查看全部领域”继续右对齐。
- 色彩与令牌：继续使用现有暖骨白、森林墨色、苔藓绿与黄绿星体，没有新增硬编码色或渐变。
- 图像质量：Logo 的真实 PNG 轨迹与星体资产、尺寸、锚点和 580ms 动画保持不变。
- 文案：红框指定的文案与编号全部不再出现；五个入口的可见文本均只为“→”，辅助技术名仍为完整“进入…章节”。
- 交互与可访问性：点击“前端”后仅前端变为 `aria-expanded=true`，再切回 AI · Agent 后仅该领域展开；主链接、搜索、主题和 Logo 动效未回退。

未发现可执行的 P0、P1 或 P2 问题。

final result: passed

## BUG-E64E8D8F 逐帧动效复验

### 视觉证据与状态归一化

- 帧序列目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，3×3 九个等时间状态，覆盖 0–580ms。
- 浏览器实现：`docs/design/qa/logo-motion-implementation-mid.png`，1265×712 像素，对应 1265×712 CSS 视口、1× 密度、浅色 `moss`、动画中段。
- 同屏聚焦对照：`docs/design/qa/logo-motion-comparison.png`，388×176 像素；左侧为 362ms 目标帧，右侧为浏览器约 350ms 的实际 Logo 区域。
- 两侧 Logo 均按 118×104 CSS px 归一化；聚焦图足以判断轨迹显现进度、星体位置、尺寸与衔接，不需要比较全页排版。

### 比较历史

**第三次用户实测，结果 blocked。** P1：星体只有 0%、44%、74%、100% 四个位置，并且相同 cubic-bezier 在每段重新起算，速度在两个中间关键帧附近明显收缩；轨迹整张蒙版平移淡入，和星体所在的曲线进度不同步。静态截图正确，但动态连续性不合格。

**逐帧修复。** 从批准的 `meteor-trail-mask.png` alpha 通道提取中心线，按等弧长生成 32 张累积轨迹帧，打包为 3776×104 的透明蒙版精灵图，控制在 4096px 纹理宽度以内以避免低端移动 GPU 分块。星体改用 9 个等弧长锚点和唯一的线性时间轴，在锚点间由浏览器连续插值；首入总时长统一为 580ms。导航悬停不再把星体瞬移回轨迹起点，只触发 360ms 原地轻闪。

**第四次对照，结果 passed。** 浏览器以约 64ms 间隔采样 10 次：轨迹蒙版位置依次为 0%、16.129%、32.258%、48.387%、64.516%、80.645%、96.774%、100%，星体矩阵在同一方向连续逼近 `(0,0)`，中间没有倒退、停驻或 easing 重启。362ms 目标帧与浏览器约 350ms 实现帧的轨迹长度、星体位置和尺度一致；桌面与 390×844 移动端均无横向溢出，控制台为空。

### 五项表面检查

- 字体与层级：未修改页面字体、字重、字号、行高或文案层级。
- 间距与布局：首页标识仍为 118×104，星体仍为 48×48；390px 端为 74×66 / 32×32，布局宽度 375px、scrollWidth 375px。
- 色彩：继续使用主题令牌中的苔藓绿轨迹与黄绿色星体，没有新增渐变或蓝紫色。
- 资产质量：轨迹 32 帧由批准的真实 PNG alpha 累积生成；星体继续使用原 PNG 蒙版。没有 CSS 图形、字符或手绘 SVG 替代。
- 文案：无变化。

### 交互与无障碍

- 首页首入使用 32 帧轨迹 + 9 锚点星体，统一 580ms。
- 导航 hover/focus 只播放 `vp-star-glint`，轨迹保持静止，避免重播时的瞬移跳变。
- `prefers-reduced-motion: reduce` 下取消两类动画，直接显示完整 Logo。

final result: passed

## BUG-722A6BD4 星体锚点复验

### 视觉证据与状态归一化

- 帧序列目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，浅色 `moss`、1× 密度。
- 桌面实现：`docs/design/qa/logo-motion-implementation-mid.png`，1265×712 像素，对应 1265×712 CSS 视口、动画约 350ms。
- 同屏聚焦对照：`docs/design/qa/logo-motion-comparison.png`，388×176 像素；左右均归一化到 118×104 标识槽位。
- 移动端实现：`docs/design/qa/logo-motion-mobile-after.png`，390×844 像素，对应 390×844 CSS 视口、完整静止状态。

### 比较与回归

- 用户反馈：星体在轨迹末端的位置偏左，需要轻微右移，不改变现有曲线、尺寸或动画节奏。
- 调整：118×104 首页标识中的 48×48 星体向右移动 4px；96×84 与 74×66 响应式标识分别右移 3px；导航与页脚小标识分别右移 2px 和 1px。
- 连续性：星体的 9 个动效锚点相对最终布局计算，因此所有中间帧与静止终点整体同步右移，没有首尾跳位。重新生成的同屏对照未发现轨迹断裂或星体越界。
- 五项表面检查：字体、字重、文案和主题色未变化；标识槽位与页面布局未变化；真实 PNG 蒙版保持清晰；仅星体横向定位发生调整。
- 浏览器回归：桌面 clientWidth / scrollWidth 为 1250 / 1250，移动端为 375 / 375；导航 hover 只触发 `vp-star-glint`，轨迹动画为 `none`；控制台为空。
- 结果：未发现可执行的 P0、P1 或 P2 问题。

final result: passed

## BUG-72B17ECF 左移与成长动效复验

### 视觉证据与状态归一化

- 逐帧目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，九个等时间状态覆盖 0–580ms，星体由 22% 增长到 100%。
- 浏览器逐帧证据：`docs/design/qa/logo-motion-implementation-145.png`、`logo-motion-implementation-290.png`、`logo-motion-implementation-435.png`，均为 1265×712 像素、1× 密度、浅色 `moss`。
- 同屏聚焦对照：`docs/design/qa/logo-growth-comparison.png`，388×528 像素；三行分别对照前段、中段和后段，左右 Logo 均裁成 180×140 像素区域。
- 静止响应式证据：桌面 `logo-motion-implementation-mid.png` 为 1265×712 像素；移动端 `logo-motion-mobile-after.png` 为 390×844 像素。

### 比较历史

**用户更正，前一轮通过结论被取代。** 星体不应向右微调，而应相对最初锚点向左移动 10px；原缩放在 25% 时已达 90%，视觉上缺少由小变大的过程。

**本轮修复。** 桌面首页星体从当前版本向左 14px，最终落在相对最初锚点左移 10px 的位置；中屏、移动端、导航和页脚同比例调整。九个缩放节点改为 22%、34%、48%、62%、75%、86%、94%、98%、100%，透明度从 35% 连续提升到 100%，仍与 9 个等弧长位置共享 580ms 线性时间轴。

**浏览器复验，结果 passed。** 以约 55ms 间隔连续采样，星体矩阵比例依次为 0.387、0.551、0.686、0.816、0.908、0.967、0.995、1.000，没有缩小、跳帧或 easing 重启。桌面最终定位为 `right: -12px`，移动端为 `right: -9px`；桌面 clientWidth / scrollWidth 为 1250 / 1250，移动端为 375 / 375。

### 五项表面与交互检查

- 字体与文案：未修改字体、字重、行高、字距或页面文案。
- 间距与布局：首页标识仍为 118×104，星体仍为 48×48；位置左移未改变品牌区占位，桌面和移动端均无横向溢出。
- 色彩与令牌：继续使用 `--brand-trail` 与 `--brand-star`，没有新增渐变、蓝紫色或硬编码业务色。
- 资产质量：继续使用真实透明 PNG 蒙版；三段同屏对照未发现模糊、遮罩断裂或透明边缘异常。
- 交互与无障碍：导航 hover 仍只触发 `vp-star-glint`，轨迹动画为 `none`；`prefers-reduced-motion` 静态回退未变；浏览器控制台为空。

未发现可执行的 P0、P1 或 P2 问题。

final result: passed

## BUG-88EB4284 再次左移与透明渐显复验

### 视觉证据与状态归一化

- 本轮源视觉真值：`docs/design/qa/logo-fade-before.png`，1265×712 像素、1265×712 CSS 视口、1× 密度、浅色 `moss`，记录修改前的静止锚点。
- 动效目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，3×3 九个等时间状态覆盖 0–580ms；首帧星体完全透明，随后同步移动、放大和显现。
- 浏览器实现：桌面 `docs/design/qa/logo-fade-after.png` 为 1265×712 像素，移动端 `docs/design/qa/logo-fade-mobile-after.png` 为 390×844 像素；两者均为动画完成后的静止状态。
- 聚焦同屏对照：`docs/design/qa/logo-fade-position-comparison.png`，388×196 像素；左侧为修改前锚点，右侧为修改后锚点，裁切范围和密度一致。
- 全页证据用于检查整体布局与溢出；聚焦对照用于判断星体相对轨迹末端和标题的水平位置。动效过程使用浏览器自然时间采样核验，避免用稳定化截图误判中间帧。

### 比较历史

**用户追加反馈，前一轮终点与透明度规格被更新。** 星体需在当前版本基础上再向左移动 10px；入场不能从 35% 不透明度开始，而应从完全透明逐步显现。

**本轮修复。** 桌面首页 48×48 星体从 `right: -12px` 调整为 `right: -2px`；40×40 与 32×32 响应式星体分别调整为 `right: -3px` 与 `right: -2px`，导航和页脚小标识同比例调整。九个透明度节点改为 0%、8%、22%、42%、64%、82%、94%、98.5%、100%，继续与 22%→100% 缩放和 9 个等弧长位置共用 580ms 线性时间轴。

**浏览器复验，结果 passed。** 动画自然时间采样的透明度依次为 0.007、0.068、0.188、0.359、0.504、0.695、0.847、0.946、0.977，缩放矩阵依次为 0.230、0.323、0.448、0.577、0.670、0.783、0.878、0.946、0.973；两组数值都单调增长，没有闪现、回退或重新起步。桌面最终为 `right: -2px`，移动端为 `right: -2px`；桌面 clientWidth / scrollWidth 为 1250 / 1250，移动端为 375 / 375。导航 hover 仍只触发 `vp-star-glint`，轨迹动画为 `none`，控制台为空。

### 五项表面与交互检查

- 字体与文案：未修改字体、字重、字号、行高、字距或页面文案。
- 间距与布局：品牌标识占位和星体尺寸不变，仅星体水平锚点改变；桌面与移动端均无横向溢出。
- 色彩与令牌：继续使用 `--brand-trail` 与 `--brand-star`；透明渐显只改变不透明度，不引入渐变或额外颜色。
- 资产质量：轨迹与星体继续使用真实透明 PNG 蒙版；对照图未发现遮罩断裂、模糊或透明边缘光晕。
- 交互与无障碍：首入动画保持 580ms，hover/focus 与 `prefers-reduced-motion` 回退规则未变；核心导航和领域展开行为未受影响。

未发现可执行的 P0、P1 或 P2 问题。

final result: passed
