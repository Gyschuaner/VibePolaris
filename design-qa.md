# Design QA · VBP-004 R6

2026-09-06。结果：本地原型技术与设计 QA 通过；用户视觉评审待进行，不代表发布验收。

依据 R5 六格分镜与本轮「不需要涂鸦，要可维护网页」修正。将 R5 03-result.png 与 R6 第 05、06 步浏览器截图放在同一次图像输入中比较。保留模型 / Harness / 文件工具三列、逐步增加信息和正确流向；手绘文字、角色和标记改为真实文本、现有 Phosphor 图标及组件。这是用户明确要求的差异。

| 维度 | 检查与结果 |
| --- | --- |
| 布局 | 1440×900 六步、1280×800 初始及结果无横向溢出；1280 操作区底边 753.625px，小于 800px。稳定场景高度避免跳动。 |
| 字体 | 沿用全站字体，标题 27–39px，主要卡片 14px，方向标签 12px。未用图片承载文字。 |
| 色彩 | 沿用全站主题变量；浅色与深色的文件正文、请求和按钮可辨认。 |
| 图标 | 现有 Phosphor 图标；无生成涂鸦、拉伸位图或截图切页。 |
| 文案 | 每步一个标题与必要说明，补充解释和来源折叠；回答结束不等于验收通过。 |
| 动效 | 实测第 4 步未完成时仅有向右执行箭头，下一步禁用；完成时增加向左工具结果。 |

完整六步、回退清除未来答案、重播恢复初始文件、边界按钮禁用、键盘左右键、两项 details 折叠、明暗切换通过。首页选中 Harness、展开详情、进入新词条通过；首页实现未修改。

`npm run check`：Lint、类型、43/43 测试（新增 4 项）、313 页面生产构建通过。清理无用 CSS 后再构建通过，并重启本地生产预览验证。最终浏览器 warning/error 日志为空。

开发模式初次预览受跨来源资源/HMR 干扰，改用 next start 验证生产构建；未放宽来源安全配置。自动化曾遇到同名选择器报错，限定可见文本后通过，非页面故障。

没有发现阻断原型的 P0/P1/P2 问题。减少动态效果覆盖状态测试和 CSS 降级，未切换系统媒体设置实测；未做屏幕阅读器实机、手机端、真实模型/文件或 dev/生产部署。用户是否认可节奏仍待评审。

截图与复现见 [R6 记录](docs/design/VBP-004-prototype-r6/README.md)。R2 历史证据仍保留在此前目录，不作为本轮验收证据。
