# Design QA · VBP-021 侧栏方案2

2026-09-21。用户选中的第二张图已实现到现有阅读侧栏，未新增页面或依赖。此前词条演示 QA 保留在 [历史记录](docs/design/qa/before-soft-sidebar.md)。

## 视觉依据与对照

- source visual truth: `docs/design/VBP-021-soft-sidebar/selected-option.png`，1070×1470 像素，用户确认的 Image Gen 第二个展示结果。
- implementation screenshot: `docs/design/VBP-021-soft-sidebar/desktop.png`，本地3002的Harness页，1280×720 CSS视口；浏览器devicePixelRatio为2，截图工具返回1280×720像素，按CSS像素对齐。
- 状态：雾蓝背景、陶土主题、目录模式，当前章节“模型、Harness 和工具”。保留正文固定列宽与原有北极星标记。
- 全局组合见桌面截图；侧栏重点对照见 `docs/design/VBP-021-soft-sidebar/comparison.png`：左为选定稿等比缩至378×519，右为实际截图的378×520区域（x900、y40）。两图在同一输入中核对，未拉伸字体或改变页面样式。
- 对照区域包含完整侧栏和可读的顶部控件，无需额外放大区域。截图顶部的全站导航下边线、右侧系统滚动条属于裁切周边，不是侧栏边框。

## Findings 与修正历史

1. [P2，已修复] 旧 `HarnessStory` 样式额外加入64px目录上边距，导致内容与控件之间过空。初轮证据 `before-spacing-fix.png` 与DOM测量（目录top258px、margin-top64px）；首轮滚动位置导致当前章节不同，仅比较控件与目录间距，不据此判断选中状态。修复为在阅读侧栏内覆盖margin-top:0，未改变普通页面布局。
2. 修复后重新构建、重新捕获；`desktop.png`、`sidebar.png`、`comparison.png`中目录top194px，六项内容节奏与选定稿对应，无剩余P0/P1/P2差异。

## 五项视觉核对

| 项目 | 结果 |
| --- | --- |
| 字体 | 沿用系统中文无衬线；页签与目录14px，次要记笔记操作13px。选中项字重区分，无截断。原站字体和抗锯齿保持，未引入新字体。 |
| 布局 | 浅底分段控件约124×36px，右侧32px收起按钮，记一条置于次行；长竖线、页签横线与下划线移除。原有正文列、笔记横向起点固定；目录清除旧64px空白。 |
| 色彩 | 使用bg、surface、hairline、muted及accent-text现有变量；分段底色为hairline与bg混色，不硬编码雾蓝，其他主题沿用已有变量。 |
| 图像与图标 | 复用Phosphor NotePencil和Caret图标、已有品牌星星资源。页面用真实文本和控件，不将设计稿当背景。星星大小保留原站尺寸，属于既有设计约束。 |
| 文案 | 目录、批注、记一条与六项目录原文保持；无新增状态脚注或实现说明。 |

## 实际验收

- `npm run build`通过（样式修正后再次构建），未运行整套check或扩大测试。
- 真实鼠标收起、当前按钮聚焦后Space展开：正文宽834px，段落740×99.445px，正文坐标与scrollY1069.5保持一致；存在0.083px与17.325px中间位移和透明度变化，缓动保留。原始测量见 `layout-check.json`。
- 目录/批注切换正常，4条已有测试批注仍可见，四条连线终点横向偏差为0。
- 记一条可进入编辑器；仅新建一条空验收笔记并立即删除，未修改已有笔记。
- 390×844手机面板控件完整，宽390px无水平溢出，关闭与重新打开正常；证据 `mobile.png`。临时视口覆盖已清除。
- 本地3002浏览器error/warn日志为空。元素索引形式的自动化键盘聚焦曾引发工具自动滚动；改用已聚焦按钮的实际Space事件后确认页面稳定，未将工具聚焦滚动当产品问题。

## 剩余范围

未扩大到跨浏览器、屏幕阅读器实机、全部主题组合或全站回归；其余主题的现有变量映射未改动。设计稿为桌面局部，手机保留现有底部面板交互。

## Implementation Checklist

- [x] 对应用户选择的第二张图
- [x] 清理边框、分段切换与次要操作排布
- [x] 修正旧目录间距并完成再次视觉对照
- [x] 构建和限定桌面、手机交互通过

final result: passed
