# HTML / JavaScript 模板扩展设计 QA（最新：三件套课程与编号层级）

## 对照证据与归一化

- 用户标注真值：`C:\Users\chuansgu\AppData\Local\Temp\codex-clipboard-38d1bf8e-f858-4283-9831-4f1bfab13e93.png`，1146×291 像素，明确指出 CSS 词条页的 `03` 章节号与卡片 `01/02` 编号偏小，并确认以当前 CSS 结构作为后续词条模板。
- 已确认课程真值：`docs/design/qa/css-course-nav-2026-08-30/02-desktop-cascade-active.png` 与 `04-mobile-top.png`；分别用于对照桌面章节布局、滚动目录、移动端课程首屏与两列目录。
- 修改后实现：`docs/design/qa/foundation-courses-2026-08-30/01-css-learning-path-desktop.png`、`02-html-course-desktop.png`、`03-javascript-course-desktop.png`、`04-html-course-mobile.png`。
- 桌面实现均为 1250×711 像素，对应 1265×720 浏览器视口覆盖；移动实现为 375×812 像素，对应 390×844 浏览器视口覆盖。全部来自同一应用内浏览器、浅色 `moss` 主题和 1× CSS 像素输出，无密度缩放。
- 同屏比较一：用户标注局部与 `01-css-learning-path-desktop.png` 同次打开，聚焦章节号、卡片编号、标题字重和卡片间距；同屏比较二：CSS 课程桌面/移动真值分别与 HTML 课程桌面/移动实现同次打开，检查模板继承与内容差异。

## Findings

未发现仍需处理的 P0、P1 或 P2。HTML 与 JavaScript 沿用 CSS 的单栏词条、三段学习路径、六章教程和滚动目录，但分别换成“语义结构”和“事件 → 状态 → 反馈”的专属互动，不是换标题式复制。用户指出的层级编号已明显加强，同时代码编辑器行号仍保持辅助层级，没有一起放大制造噪声。

## 五项保真检查

- 字体与层级：词条章节号由 13px 提高到 20px，学习卡编号由 11px 提高到 16px；教程正文章节号由 11px 提高到 20px，课程成果编号提高到 16px，目录编号提高到 14px。标题、正文与等宽代码字体保持现有体系，无省略号、裁切或孤立单字；`.term-code-number` 仍为 10px。
- 间距与布局：词条继续使用约 930px 单栏阅读宽度，章节号与标题共享基线；课程保持 210px 目录 + 860px 内容列。390×844 实测页面 `innerWidth=390`、`scrollWidth=375`，HTML 课程目录转两列、互动内容转单列，无横向溢出。
- 色彩与令牌：全部复用 `--bg`、`--surface`、`--accent`、`--brand-star`、`--tint` 与既有代码色，不增加蓝紫渐变、边框噪声或新的强调色。
- 图像与资产：继续复用现有品牌星体蒙版与 Phosphor 图标；本轮没有新增图片、字符星、emoji、手绘 SVG、占位图或低清素材。
- 文案与内容：HTML 围绕文档骨架、语义、链接、表单和无障碍；JavaScript 围绕值、函数、事件、状态、异步和调试。案例为 VibePolaris 自己设计的“市集结构 / 收藏状态”，没有借用 VibeHub 或其他站点案例。

## 交互、响应式与无障碍

- HTML 词条三态可切换 `div → h1 → button`，`aria-pressed` 与浏览器理解结果同步；JavaScript 词条实测切到第三态后显示“页面显示 1”。
- JavaScript 教程实测第二互动阶段 `aria-pressed=true`，界面显示 `count 从 0 变成 1`；正确知识检查即时出现 `aria-live` 反馈。
- 两门教程的目录随滚动高亮；直接进入 JavaScript `#events` 时目录高亮 `03 事件与状态`，进入自测后切换为 `06 自测与下一步`。
- 390×844 下 HTML 教程六项目录完整可见，词条互动三段改为单列；新增过渡均纳入 `prefers-reduced-motion`，实际浏览器 console warning/error 为 0。

## 比较历史

1. 用户标注中词条章节号仅 13px、学习卡编号仅 11px；已确认的 CSS 课程中正文章节号也只有 11px，属于明确的 P2 层级不足。
2. 第一轮把词条章节号调至 17px、学习卡编号 14px、教程章节号 16px；实际桌面截图仍显得接近辅助文字，未满足“可以大很多”的反馈语气。
3. 第二轮提高到 20px / 16px / 20px，并把教程成果、目录和调试步骤编号同步调整；修复后同屏比较中编号可在扫读时先于说明文字被识别，而代码行号仍保持 10px。桌面、移动端和互动状态均未出现新的溢出、错位或视觉竞争。

## Implementation Checklist

- [x] 从千词候选中正式提升 JavaScript，并与 HTML 组成结构 / 样式 / 行为三件套
- [x] HTML 与 JavaScript 专属词条故事、互动状态、判断题、提示词和系统教程入口
- [x] 两门六章深度教程、滚动目录、互动练习、自测、站内下一步与权威资料
- [x] 放大词条与教程的结构编号，同时保持代码行号为辅助层级
- [x] 1265×720 与 390×844 浏览器验收，核心互动、目录状态和 console 已检查

## Follow-up Polish

- P3：当课程数量继续增长时，可把 `Course 01/02/03` 自动来自课程清单，避免后续人工调整序号；不影响本轮使用。

final result: passed

---

# CSS 深度教程设计 QA（最新：滚动目录与阅读字号）

## 对照证据与归一化

- 用户标注真值：`C:\Users\chuansgu\AppData\Local\Temp\codex-clipboard-5abc5421-5eb5-4fc1-be4a-2695971843ea.png`，1140×255 像素，明确指定删除首屏说明句与三项课程元信息。
- 修改前全屏真值：`docs/design/qa/css-course-2026-08-30/01-course-desktop.png`，1250×711 像素；用于保持已经确认的版式、品牌、标题与内容结构。
- 修改后实现：`docs/design/qa/css-course-nav-2026-08-30/01-desktop-top.png`、`02-desktop-cascade-active.png`、`03-desktop-debug-active.png` 与 `04-mobile-top.png`。
- 桌面截图为 1250×711 像素，对应 1265×720 浏览器视口覆盖；移动截图为 375×812 像素，对应 390×844 浏览器视口覆盖。全部来自同一应用内浏览器、浅色 `moss` 主题和 1× CSS 像素输出。
- 同屏比较在同一次图像输入中依次打开用户标注、修改前首屏和修改后 `01-desktop-top.png`；聚焦比较使用 `02`、`03` 验证目录高亮与正文小字，`04` 验证移动端重排。

## Findings

未发现仍需处理的 P0、P1 或 P2。用户指出的说明句与“6 章 / 约 35 分钟 / 3 个动手节点”已从课程首屏完全移除；左侧目录从静态小字列表升级为随阅读位置自动切换的 15px 导航，当前章使用浅绿色承载面、苔藓绿字重与 `aria-current="location"` 同步表达。

## 五项保真检查

- 字体与层级：目录正文由 12px 提高到 15px、编号由 10px 提高到 12px；规则、Computed、步骤卡、调试卡、资源卡等 9–12px 的主要阅读文字提高到 11–15px。标题字阶和系统中文字体保持不变，无截断和省略号。
- 间距与布局：删除首屏冗余后收紧 hero 下边距，课程价值块更早进入视口；目录项提高到 52px，但 210px 目录列与 860px 内容列未改变。390×844 实测页面 `scrollWidth=375`，无横向溢出。
- 色彩与令牌：活动目录只复用现有 `--tint`、`--accent`、`--brand-star`，没有增加线框、阴影、渐变或新的强调色。
- 图像与资产：品牌流星、星体蒙版与现有 Phosphor 图标均未改变；本轮无新增或替换图像资产。
- 文案与内容：只删除用户点名的首屏说明与课程统计，不改六章正文、互动示例、知识检查或权威资料。

## 交互、响应式与无障碍

- 首屏实际状态为 `#rule`；纯滚动到第二章后自动切为 `#cascade`；直接进入 `#debug` 后自动切为 `#debug`；滚动到底部会切为 `#practice`。
- 当前目录项同时提供 `.is-active` 与 `aria-current="location"`；点击锚点立即响应，滚动和窗口尺寸变化会重新计算阅读位置。
- 390×844 下目录保持两列，六项均为 52px 高、15px 字号；首屏冗余内容不存在，课程价值块紧接目录出现。
- 新增目录过渡继续受 `prefers-reduced-motion` 规则控制；浏览器实际操作后的 console warning/error 为 0。

## 比较历史

1. 修改前首屏包含一段重复说明和三项课程统计，且 12px 目录与 9–11px 功能块文字在桌面上显得偏弱，属于用户明确指出的 P2 信息噪声和可读性问题。
2. 第一轮修复删除两组首屏内容，放大目录与主要功能块文字，并增加基于阅读线的滚动目录状态。
3. 修复后在桌面依次验证首屏、第二章和调试章，在移动端验证首屏与两列目录；没有出现新的横向溢出、断行、遮挡或无效高亮。

## Implementation Checklist

- [x] 删除首屏说明与课程统计
- [x] 滚动位置驱动目录高亮，支持初始 hash、点击和页面底部
- [x] 放大目录与正文功能块小字
- [x] 1265×720 与 390×844 浏览器验收，console 0 warning/error
- [x] ESLint、TypeScript、19 项 Node 测试和静态生产构建

## Follow-up Polish

- P3：当前滚动阅读线设在视口高度 30%、最大 260px；如果后续加入更短的章节，可以按真实阅读反馈微调，而不需要改变目录结构。

final result: passed

---

# CSS 学习架构设计 QA（最新：词条入口 + 深度教程）

## 对照证据与归一化

- 视觉真值：`docs/design/qa/responsive-star-story-2026-08-30/01-desktop-top.png` 与 `05-mobile-top.png`。本轮不是复刻新页面，而是把用户已确认的 CSS 词条页作为字体、色彩、品牌资产、密度与阅读节奏的视觉真值。
- 浏览器实现：`docs/design/qa/css-course-2026-08-30/01-course-desktop.png`、`02-course-lab.png`、`03-term-learning-entry.png`、`04-course-mobile.png` 与 `05-course-mobile-lab.png`。
- 桌面源图与实现均为 1250×711 像素，对应浏览器 1265×720 视口覆盖；移动源图与实现均为 375×812 像素，对应浏览器 390×844 视口覆盖。截图均为同一应用内浏览器、浅色 `moss` 主题、1× CSS 像素输出，不需要密度缩放。
- 状态：词条页滚动到“继续学习”底部；教程页分别覆盖首屏、选择器练习的整卡选中态和正确答案态。全视图比较在同一次图像输入中并置桌面源图与 `01-course-desktop.png`，移动比较同样并置移动源图与 `04-course-mobile.png`；练习区另用 `02-course-lab.png` 与 `05-course-mobile-lab.png` 做聚焦检查。

## Findings

未发现仍需处理的 P0、P1 或 P2。词条页保留轻量的“接下来学什么”，新增一个高辨识度的站内教程入口和三个低权重权威外链；专业教程页沿用暖骨白、苔藓绿、黄绿色星体、系统中文字体和低噪分段，但用课程目录、互动代码、Computed 结果与自测承载系统内容，没有把词条页堆成文档目录。

## 五项保真检查

- 字体与层级：桌面教程标题使用与现有词条标题一致的紧凑黑色大字，正文继续使用系统中文字体，代码保持等宽；移动端为避免“改样式”被生硬拆开，标题改为同义短句“CSS，先看懂 / 浏览器怎么决定”，没有截断或省略号。
- 间距与布局：桌面为 210px 固定目录 + 860px 阅读列，章节之间只保留必要分隔；390×844 下目录改为两列 44px 触达项，所有示例转单栏。实测 `innerWidth=390`、页面 `scrollWidth=375`，无横向溢出。
- 色彩与令牌：全部表面使用既有 `--bg`、`--surface`、`--accent`、`--brand-star`、`--tint` 与代码令牌；没有蓝紫 AI 渐变、新配色或装饰性网格。
- 图像与资产：页头、教程入口和互动预览继续复用已批准的 `polaris-star-mask.png` 品牌蒙版；没有新增字符星、emoji、手绘 SVG、占位图或生成图片。
- 文案与内容：课程按“规则与选择器 → 层叠最终值 → Flex/Grid → 响应式 → 浏览器调试 → 自测”组织，所有例子都来自 VibePolaris 自己的教学语境；三个外部入口只承担继续查证，不借用第三方站点案例。

## 交互、响应式与无障碍

- 选择器练习实测三种按钮会同步更新代码、页面高亮和 Computed；`aria-pressed` 从标题切到直接子段落后分别为 `false / true`，预览 class 同步为 `is-target-copy`。
- 知识检查实测错误与正确反馈均可见且使用 `aria-live`；正确选项回显“先确认当前值从哪里来，再用 CSS 在正确的断点重排”。
- CSS 词条页站内入口真实指向 `/guides/css`，三条外链均为新窗口权威资料；教程页的“回到词条”和“下一颗星”是真实内部链接。
- 移动端目录项为 44px，练习标签为 58px；代码和预览上下堆叠。`prefers-reduced-motion` 关闭新增控件的过渡，不影响可读状态。
- 桌面与移动端交互完成后，应用内浏览器 console warning/error 为 0。

## 比较历史

1. 第一轮移动截图中常规导航的“Vibe指北”被挤成两行，同时长标题把“改样式”拆成不自然的断句，属于 P2 的移动端品牌与标题节奏问题。
2. 修复时教程页改用与词条页一致的宽版导航，在 560px 以下隐藏副名；移动标题使用同义短句而非强行缩小桌面文案。`04-course-mobile.png` 复验确认品牌单行、标题两行完整，目录与首个内容区仍在合理滚动距离内。
3. 修复后再次并置桌面与移动视觉真值；暖骨白画布、品牌星体、标题字重、绿色功能面与低噪间距一致，没有产生新的 P0/P1/P2。

## Implementation Checklist

- [x] CSS 词条页新增站内深度教程和三个权威外部学习入口
- [x] `/guides/css` 六章教程、固定目录、互动选择器、Computed 与知识检查
- [x] 桌面 / 390px 响应式、44px 触达尺寸和 reduced-motion 回退
- [x] ESLint、TypeScript、19 项 Node 测试、静态生产构建和应用内浏览器验收

## Follow-up Polish

- P3：当前教程内容以建立判断方法为主；后续可以沿用同一骨架继续补充盒模型、定位、变量与动画四个进阶章节，但不建议一次性全部堆入首版。

final result: passed

---

# CSS 词条页设计 QA（最新：原创响应式星图故事）

## 对照证据与归一化

- 视觉真值：`docs/design/audit/term-page-comparison-2026-08-30/11-top-comparison.png`、`12-code-comparison.png` 与 `14-mobile-comparison.png`。它们保留 VibeHub 的信息节奏和 VibePolaris 既有暖骨白 / 苔藓绿 / 星体品牌表面，只作为结构与密度边界，不复用对方的教学案例。
- 浏览器实现：`docs/design/qa/responsive-star-story-2026-08-30/01-desktop-top.png`、`04-code-wrap-fixed.png`、`05-mobile-top.png`、`06-mobile-demo.png` 与 `08-mobile-zero-fill-prompt.png`。
- 桌面实现截图为 1265×720 像素，对应 1265×720 CSS 视口；移动实现截图为 390×844 像素，对应 390×844 CSS 视口。应用内浏览器截图均按 1× CSS 像素输出，无拉伸；滚动条保留区不计作页面横向溢出。
- 状态：浅色 `moss`，路由 `/terms/css?v=responsive-star-story-final`；全视图为“桌面展开”，聚焦视图为手动暂停后的“问题出现”，移动端同时覆盖首屏与演示区。
- 全视图比较在同一次图像输入中打开 `11-top-comparison.png` 与 `01-desktop-top.png`；聚焦比较在同一次图像输入中打开 `12-code-comparison.png` 与 `04-code-wrap-fixed.png`；移动比较同样并置 `14-mobile-comparison.png` 与 `05-mobile-top.png`。

## Findings

未发现仍需处理的 P0、P1 或 P2。新版保留参考结构的单栏阅读节奏、问题引入、可操作示例、判断题和学习路径，但把通用按钮覆盖案例换成 VibePolaris 自己的“技术星图响应式重排”：桌面三点展开、390px 三列挤压、媒体查询修正为单列。代码、视口宽度、星点位置和 Computed 列数由同一状态同步切换。

## 五项保真检查

- 字体与层级：页面继续使用项目系统中文字体和既有标题字阶；代码使用等宽字体，并改为真正的多行规则，不再用省略号截断。390px 首屏定义为 23px / 34.04px 行高，长句自然换行。
- 间距与布局：桌面为代码 / 星图双栏，390px 转为单栏；实测桌面与手机均无横向溢出。原有网格背景已移除，演示区只保留一个浅色承载面和一个深色代码面。
- 色彩与令牌：暖骨白、苔藓绿、黄绿色星点和深苔藓代码面均来自现有主题令牌；没有参考站的蓝色主调、渐变或新增的高噪装饰线。
- 图像与资产：星图节点与页头继续复用已批准的 `polaris-star-mask.png` 品牌蒙版；没有用字符星号、emoji、手绘 SVG 或占位图代替品牌资产。
- 文案与内容：问题、示例和判断题围绕同一星图窄屏问题；AI 提示词则抽象为“检查当前页面”的零填空诊断指令，自动查找重叠、溢出、拥挤和排列异常，不要求读者理解或替换模板槽位。内容不再借用参考站案例，也没有无关推广模块。

## 交互、响应式与无障碍

- 演示每 2000ms 推进一次，只在可见且处于播放状态时运行；点击任一阶段会暂停，播放与重播按钮均有可访问名称，`prefers-reduced-motion` 下停用自动播放和过渡。
- 浏览器实际验证“桌面展开 → 问题出现 → 响应式修正”：预览 class 分别为 `is-desktop / is-squeezed / is-stacked`，Computed 同步从 `repeat(3, 1fr)` 切换到 `1fr`。
- 390×844 实测标签页与播放控制均为 44px 高，朗读为 44×44，移动端 Markdown 复制入口为 44×44；代码与 Computed 均没有 `text-overflow: ellipsis`。
- 判断题正确反馈、提示词复制到剪贴板、MDN 外链与学习路径均已操作验证；最终控制台 warning/error 为 0。

## 比较历史

1. 第一轮使用“灰色按钮被规则覆盖”的通用案例，结构清楚但与参考站案例过近，属于 P1 的产品辨识度问题。按用户反馈删除整套案例，改为项目原创的技术星图响应式故事。
2. 原创案例第一版为了维持单行代码，使用 `text-overflow: ellipsis`；用户截图指出关键 CSS 被截断，属于 P2 的教学信息损失。
3. 修复后删除重复的行尾状态文字，把三条规则改为 3–5 行真实代码排版，同时允许 Computed 自然换行。`04-code-wrap-fixed.png` 与 DOM 复验确认全部规则完整可见，桌面和 390px 均无横向溢出或省略号。
4. 第二轮内容复核发现提示词仍把读者锁在“技术星图”业务场景，属于 P2 的知识迁移障碍。先改成可替换槽位模板，但继续复核后确认填空仍增加使用成本；最终改成零填空的“检查当前页面”指令。`08-mobile-zero-fill-prompt.png` 与剪贴板回读确认无占位符、可直接复制使用。

## Implementation Checklist

- [x] 原创技术星图“桌面 → 挤压 → 响应式修正”三阶段
- [x] 代码、星图位置、视口宽度与 Computed 同步
- [x] 完整多行代码、无省略号和 44px 移动端触达尺寸
- [x] 判断题、提示词复制、学习路径与 MDN 查证入口
- [x] ESLint、TypeScript、16 项 Node 测试、静态生产构建和应用内浏览器验收

## Follow-up Polish

- P3：移动端代码字号为了完整展示保持在 10.5px；若后续真机反馈阅读压力较大，可把代码面改成可横向拖动的独立区域，但不应再次截断内容。

final result: passed

---

# CSS 词条页设计 QA（最新：代码映射动效）

## 对照证据与归一化

- 视觉真值：`docs/design-qa/css-reference-1340x935.png`，1340×935 像素；原始视觉来自 `C:\Users\chuansgu\.codex\generated_images\01a04cd2-3ffd-71a3-9eca-5d7f98f45830\exec-f869a214-4d4c-490e-877c-2f47730d5293.png`。
- 浏览器实现：`docs/design-qa/css-implementation-1340x935.png`，1340×935 像素；CSS 内容视口为 1340×945，浏览器 `devicePixelRatio=1.5`，截图工具归一化为 1× CSS 像素并裁去浏览器保留区。
- 聚焦状态：`docs/design-qa/css-code-map-active.png`，1340×935 像素；“基础样式”面板中手动选中 `h1 { color: #35502B; }`，自动播放已暂停。
- 验收页面：`http://localhost:3000/terms/css?v=code-map-final`，浅色 `moss` 主题。视觉真值没有定义代码映射动效，因此全视图用于确认既有版式和品牌表面未漂移，聚焦截图与 DOM 同步采样用于确认新增交互本身。

## Findings

未发现仍需处理的 P0、P1 或 P2。新增区域保持参考稿的单栏阅读结构、暖米白画布、黄绿色强调和低噪分段；代码编辑器是唯一深色功能表面，右侧预览仍是原有示例页面，没有引入蓝紫渐变、卡片堆叠或解释性长文。

## 五项保真检查

- 字体与层级：页面标题、节标题、正文与标签继续沿用项目字阶；代码区单独使用系统等宽字体。代码行、目标标签和状态文案均无截断，移动端只隐藏重复的行尾中文标签。
- 间距与布局：桌面为代码 0.88fr / 预览 1.12fr 双栏，390px 端转为单栏；手机实测 `scrollWidth=clientWidth=375`。原页面标题、引导区、判断题和底部内容节奏未被新面板推挤成密集信息流。
- 色彩与令牌：代码区使用深苔藓背景、浅绿语法色和 `--brand-star` 当前行指示；页面仍消费既有暖骨白、苔藓绿与黄绿色主题令牌，没有蓝紫色或渐变。
- 图像与资产：第 4 步内容留白变化后才显现已批准的 `css-preview-sprig.png`；品牌星体和植物资产均未被字符、emoji、手绘 SVG 或占位图替换。
- 文案与内容：四条代码分别映射“页面背景 / 标题颜色 / 导航排列 / 内容留白”，状态栏同时显示中文目标与 `selector → property`，无需额外说明段落也能理解对应关系。

## 交互、响应式与无障碍

- 面板进入视口后每 1500ms 推进一条代码；只在可见且处于播放状态时运行，离开视口即停止计时。
- 点击任意代码行会暂停并锁定该状态；播放、暂停和重播均使用原生按钮和可访问名称。浏览器实测恢复播放会继续推进，重播会回到第 1 行。
- 当前代码行、预览组件 class 和状态栏在同一 React 状态更新中切换；聚焦截图中 `h1` 行、标题高亮和 `h1 → color` 完全对应。
- `prefers-reduced-motion` 下不启动计时器，关闭代码区动画与过渡，并隐藏无意义的播放控件；静态内容仍完整可读。
- 390×844 移动视口完成堆叠、点选和无横向溢出验收；最终全新导航后的控制台 warning/error 为 0。

## 比较历史

1. 第一轮动效捕捉到代码行的背景与位移动画会保留旧选中态约 220ms，而右侧状态已进入下一条，属于 P2 的短暂映射歧义。
2. 修复时移除代码行背景、阴影和位移的缓动，只保留不影响对应关系的颜色反馈；页面区域继续使用合成友好的平滑动画。
3. 修复后手动选中第 2 行并重新截图，代码高亮、`code-step-1 is-highlight-title`、标题视觉反馈和 `h1 → color` 状态在同一帧一致；桌面和手机未出现新的 P0、P1 或 P2。

## Implementation Checklist

- [x] 四步代码到页面区域的可视映射
- [x] 可见区自动播放、手动点选暂停、继续与重播
- [x] 桌面双栏、移动端单栏和无横向溢出
- [x] `prefers-reduced-motion` 静态回退
- [x] ESLint、TypeScript、16 项 Node 测试、静态生产构建与应用内浏览器验收

## Follow-up Polish

- P3：当前每步固定停留 1500ms；后续可根据真实用户观察结果微调为 1800–2200ms，但不建议加入更多装饰性动效。

final result: passed

---

# VibePolaris 设计 QA（最新：词条详情页“双栏词典”）

## 词条详情页“双栏词典”

### 对照证据与归一化

- 视觉真值：`D:\Pythonproject\VibeP\docs\design\VBP-001-term-detail-split-lexicon.png`，1487×1058 像素；这是用户在本轮三套词条详情方案中选择的第 2 套。
- 生产构建实现：`D:\Pythonproject\VibeP\docs\design\qa\VBP-001-term-detail-implementation.png`，1265×712 像素；应用内浏览器 CSS 视口为 1280×720，设备像素比 1.5，截图包含 15px 原生滚动条占位。
- 全视图同屏对照：`D:\Pythonproject\VibeP\docs\design\qa\VBP-001-term-detail-comparison.png`。视觉真值等比缩放到 1265px 宽后裁取同一 712px 顶部视口，与实现并排放置；没有拉伸或非等比缩放。
- 聚焦区域同屏对照：`D:\Pythonproject\VibeP\docs\design\qa\VBP-001-term-detail-focus-comparison.png`。分别比较左侧星体 / 标题 / 分类 / 定义，以及右侧“大白话”“它是什么”和首屏提示词入口；这些区域以原始截图像素裁取，没有二次缩放。
- 状态：浅色 `moss`，路由 `/terms/tools`，默认静止状态；复制成功态、首页聚焦后二次点击进入词条、相关术语跳转另行实测。

### Findings

未发现仍需处理的 P0、P1 或 P2。实现保留了参考稿的约 32% / 68% 双栏比例、唯一竖向分隔、左侧大星与词条身份、右侧三段阅读内容、浅色提示条和低噪相关术语入口；没有新增卡片网格、阴影、蓝紫色、渐变或多余解释区。

### 五项保真检查

- 字体与层级：沿用项目现有系统中文字体和字重；主词条为唯一超大标题，英文名使用苔藓绿次级层级，右侧 24px 小节标题与 15–16px 正文保持稳定阅读节奏。没有截断、异常换行或过密灰字。
- 间距与布局：桌面分隔线、左侧星体、标题基线、右侧三个小节的纵向位置与参考稿一致。参考稿和实现只保留一条结构分隔线；小节下方改为 28px 短标线，避免整段下划线增加噪声。
- 色彩与令牌：实现只消费现有 `--bg`、`--ink`、`--muted`、`--hairline`、`--accent`、`--brand-star` 和 `--tint`；暖骨白、深苔藓绿和黄绿色星体与参考稿一致，浅色与暗色主题继续由既有令牌驱动。
- 图像与资产：左侧主星、相关术语星点和跨路由落点全部复用项目已批准的 `polaris-star-mask.png` 真资产；没有使用字符星号、emoji、内联 SVG、手绘 SVG、渐变或占位图。
- 文案与内容：本轮按用户要求不重写正文，继续使用结构化术语数据和现有示例；视觉层级已为后续内容设计预留“大白话 / 它是什么 / 向 AI 这样说 / 相关术语”四个稳定槽位。

### 交互、响应式与无障碍

- 浏览器实际完成：AI · Agent → 工具调用聚焦 → 二次点击进入 `/terms/tools`，复制按钮进入“已复制”成功态，相关术语 `Token` 跳转到 `/terms/token` 后可返回。
- 跨路由流星继续读取 `data-route-star-target` 的真实位置，因此首页概念星会落到左栏放大的主星，而不是旧版标题旁的小星。
- 词条分类与相关术语使用真实链接，复制使用原生按钮；hover / focus-visible 均有可见反馈，`prefers-reduced-motion` 继续关闭路由与星体过渡。
- CSS 已提供 900px 单栏重排和 560px 紧凑排版；本轮所选视觉只有桌面稿，应用内浏览器没有暴露可切换的移动视口，因此没有新增移动端视觉对照图。该缺口不影响桌面视觉真值的通过结论，保留为后续响应式专项复验项。
- 生产构建浏览器控制台日志为空。

### 比较历史

1. 第一轮实现中，`term-route-star` 的旧 54px 规则覆盖了新主星尺寸，实际星体远小于参考稿，属于 P1 品牌焦点缺失；同时左侧标题和右侧后两段内容整体偏下，属于 P2 节奏漂移。
2. 修复时提高词条主星选择器优先级并复用星体蒙版的批准裁切比例，使可视星体接近参考稿的 140px；同时压缩标题、分类、定义与章节间距，第二次同屏对照中左右栏关键基线一致。
3. 第二轮仍使用整段标题下划线，视觉上比参考稿更重，也不符合项目的降噪原则，属于 P2 表面处理偏差；最终改成 28px 短标线并重新构建、截图和对照。最终同屏和聚焦对照没有发现新的 P0、P1 或 P2。

### Open Questions

- 正文示例、准确解释和 Prompt 模板将按用户要求在下一轮单独设计；当前只确认信息槽位和视觉层级。

### Implementation Checklist

- [x] 双栏词典结构与宽版词条页导航
- [x] 左侧大星、标题、分类、定义与相关术语
- [x] 右侧大白话、定义、可复制 Prompt 的低噪阅读列
- [x] 首页流星落点、复制成功态和相关术语导航
- [x] 900px / 560px 响应式样式、reduced-motion 回退
- [x] ESLint、TypeScript、13 项 Node 测试、静态生产构建和浏览器验收

### Follow-up Polish

- P3：参考稿中的示例对话更具体，当前继续沿用通用内容；下一轮做内容设计时再统一示例语气、定义长度和 Prompt 模板。
- P3：待有移动端视觉稿后，补一轮 390×844 同屏对照并微调相关术语在单栏中的位置。

final result: passed

---

## 历史：首页 R4.3 左锚展开

## 首页 R4.3 左锚展开

### 对照证据与归一化

- 视觉真值：`docs/design/qa/left-anchor-2026-08-30/source-option1.png`，1487×1058 像素；这是用户选择的第 1 套四阶段动效分镜，目标画布为 1440×1024。
- 生产构建实现：`docs/design/qa/left-anchor-2026-08-30/12-production-focus.png`，1265×712 像素；浏览器 CSS 视口为 1265×720，设备像素比 1.5，截图工具按 1× CSS 像素输出。
- 全分镜同屏对照：`docs/design/qa/left-anchor-2026-08-30/13-comparison-full.png`。视觉真值等比缩放到 1265px 宽后与生产截图纵向拼接，没有拉伸。
- 聚焦区域同屏对照：`docs/design/qa/left-anchor-2026-08-30/14-comparison-focus.png`。左侧裁取分镜的最终“可继续”状态，右侧裁取实现中的左锚标题与右侧知识摘要，两侧统一到 620px 高后比较层级、比例、色彩和文案。
- 动效逐帧数据：`docs/design/qa/left-anchor-2026-08-30/motion-samples.json`。进入锚定阶段后连续采样 58 帧，最大帧间隔 18ms；选中概念的横向位置连续由 266px 收敛至 203px，外围“上下文”连续由 112px 散至 92px，没有倒退、停驻或分段跳变。
- 移动端证据：`docs/design/qa/left-anchor-2026-08-30/16-mobile-frame-focus.png`；390×844 iframe 中实际内容视口为 375×844，`scrollWidth=clientWidth=375`、`scrollHeight=clientHeight=844`，最终聚焦内容与入口均可见。

### Findings

未发现仍需处理的 P0、P1 或 P2。实现与选定分镜一致：概念先在原位置点亮并轻微放大，再连续移动到左侧阅读锚点；其他标签按各自方向散到外围并降噪；右侧依次出现定义、“它解决什么”、“怎么工作”、“相关概念”和唯一的完整词条入口。页面没有卡片、连接线、蓝紫渐变或新增阴影。

### 五项保真检查

- 字体与层级：沿用项目现有系统中文字体和字重令牌。左侧选中概念保持品牌绿与大字号，右侧定义为唯一黑色强标题，三组说明使用较小字号；没有截断、异常换行或多余灰字。
- 间距与布局：桌面最终状态为左侧约 23.5% 锚点、右侧 52% 内容列，外围标签落在独立安全槽位。1265px 桌面和 375px 移动内容视口均无横向溢出，移动端自动转为上方概念、下方摘要。
- 色彩与令牌：只使用既有 `--surface`、`--ink`、`--muted`、`--accent`、`--brand-star`；暖骨白、苔藓绿和黄绿色星体与分镜一致。
- 图像与资产：继续复用项目批准的品牌星体 / 轨迹真实蒙版资产，没有新增 emoji、字符星号、内联 SVG、手绘 SVG、渐变或占位图。
- 文案与内容：当前 AI · Agent 七个概念提供定制摘要；其他技术分类从结构化术语数据生成定义、三步理解方式与关联词。内容比旧版单句更完整，但仍保持三组短信息和一个入口。

### 交互、性能与无障碍

- 状态机为 `primed → anchoring → revealed`：0–120ms 原位点亮，120–520ms 左锚与外围散开，520ms 后右侧内容按 55/115/170ms 错峰显现；`prefers-reduced-motion` 直接进入最终状态。
- 浏览器实际完成：AI · Agent → 工具调用聚焦、二次点击进入 `/terms/tools`、前端 → CSS 通用详情、Escape 退出聚焦、生产构建无横向溢出和移动端聚焦。
- 聚焦按钮继续使用原生 `button`、`aria-pressed`、动态 `aria-label` 与 `aria-live`；Escape 可恢复星图。右侧入口与重复点击均复用现有跨路由流星。
- 页面控制台日志没有 warning/error；生产构建没有 Next.js 错误覆盖层。ESLint、TypeScript、13 项 Node 测试与静态生产构建全部通过。

### 比较历史

1. 修改前 `docs/design/audit/concept-focus-2026-08-30/04-final-focus.jpg` 中概念直接跳到页面中心，外围只做统一淡化，详情只有一句摘要，属于 P1 交互模型不符合本轮明确选择。
2. 修复后增加独立聚焦阶段和每个槽位的外围目标坐标；选中概念先原位响应再左移，右侧改为分层内容，移动端改为上下阅读结构。
3. 生产构建复验和两张同屏对照中没有发现新的 P0/P1/P2；二次点击详情、通用概念和 Escape 均通过回归。

### Open Questions

- 无。

### Implementation Checklist

- [x] 原位点亮与左锚连续位移
- [x] 外围标签错向散开与低噪 hover
- [x] 分层摘要、唯一入口与二次点击跳转
- [x] reduced-motion、键盘退出和移动端重排
- [x] 自动化检查、生产构建和浏览器回归

### Follow-up Polish

- P3：实际桌面把右侧定义字号略放大于分镜中的缩略帧，以保证 1265px 视口可读性；如果后续希望更“杂志化”，可在不改变交互模型的前提下继续微调左右列的垂直基线。

final result: passed

---

## 历史：首页 R4.2 跨路由流星

### 动效结构

- 单击已有详情页的概念后，先复用 R4.1 的星位交换：概念星在 470ms 内进入中心，分类中心星回到原概念槽位。
- 概念星不在中心停住，继续进入 760ms 的跨路由飞行；280ms 时预取完成的 Next.js 路由开始切换，星体在根布局中保持为同一个飞行层，不随页面卸载。
- 飞行轨迹使用二次曲线采样的三个路径节点，尾迹继续使用品牌 `meteor-trail-mask.png`，详情页真实星体作为最终落点。新页面出现后会读取实际落点位置，避免响应式布局带来的估算偏差。
- 页面只在星体飞行时短暂降低主体透明度并下沉 9px，导航保持稳定；飞行结束后移除覆盖层，详情页标题左侧只保留一颗 54px 真实北极星。

### QA 结果

- 桌面应用内浏览器完成“前端 → CSS”单击跳转，依次检查换位、跨页中段、落地和静止状态；最终 URL 为 `/terms/css`，飞行层数量为 0，目标星体数量为 1、不透明度为 1。
- 390×844 iframe 完成同一链路；落地后 `scrollY=0`，飞行层数量为 0，标题与 42px 星体正常换行排布，未见横向溢出。
- 组件、状态、响应式、表单、API、数据库、认证及原有 AI / 技术栈 / 产品详情入口同步复用这套动效；没有详情页的概念仍停留在中心聚焦，不伪造空详情页。
- CSS 新增为第 21 条结构化术语，生产构建静态生成 30 个页面。ESLint、TypeScript、13 项 Node 测试和生产构建全部通过。
- `prefers-reduced-motion` 的运行分支会跳过星位和跨页飞行，直接调用路由；本轮由代码断言与全局减少动态样式覆盖，应用内浏览器不提供系统媒体偏好切换入口。

未发现仍需处理的 P0、P1 或 P2 问题。

final result: passed

---

## 首页 R4.1 星位交换动效

### 实现与证据

- 概念点击：两颗真实北极星蒙版沿浅弧线交换位置，中心与来源文字先淡出，210ms 时更新语义内容，470ms 完成并移除飞行图层。
- 分类切换：旧星点向中心收拢淡出，150ms 后替换数据，新星点按 24ms 错峰从内向安全槽位散开。
- 静止状态：只保留一颗背景星每 7.6s 短暂闪烁，中心星每 8.4s 轻微变亮；不做持续漂浮或全场呼吸。
- 逐帧样本：`docs/design/qa/homepage-motion-swap-mid.png`、`homepage-motion-focus-end.png`、`homepage-motion-domain-end.png`。

### QA 结果

- 首版共享视图过渡会拉伸整块文字和焦点轮廓，并在连续交互时记录 `InvalidStateError`；已改为只动画两颗星体的独立飞行图层，不再缩放文字截图。
- 应用内浏览器实测概念聚焦、返回中心、Escape、分类聚散和 45ms 连续切换；最终仅保留 1 个中心星节点、飞行图层为 0，控制台日志为空。
- 390×844 真实 iframe 视口完成概念换位，中心链接、星体数量和排布正常。
- `prefers-reduced-motion` 下不创建飞行图层，分类和焦点状态直接更新。

final result: passed

---

## 首页 R4 极简技术星图

### 对照证据

- 视觉真值：`C:\Users\chuansgu\.codex\generated_images\01a04cd2-3ffd-71a3-9eca-5d7f98f45830\exec-a9bfb52e-e607-4d4c-8e19-8ac8d2918652.png`；项目内副本 `docs/design/qa/homepage-star-map-reference.png`。
- 视觉真值像素：1487×1058；生成目标画布为 1440×1024。
- 桌面实现：`docs/design/qa/homepage-star-map-implementation-1280x720.png`；应用内浏览器 CSS 视口与截图像素均为 1280×720，浅色 `moss`、AI · Agent 默认中心态。
- 同屏对照：`docs/design/qa/homepage-star-map-comparison.png`。两侧各放入 640×455 等大容器，使用 contain 等比缩放、不拉伸；由于应用内浏览器桌面视口固定为 1280×720，本轮按同一内容状态比较构图与层级，不做逐像素位置断言。
- 聚焦状态：`docs/design/qa/homepage-star-map-context-focus.png`；上下文进入中心，AI · Agent 回到周边星点。
- 分类状态：`docs/design/qa/homepage-star-map-backend.png`；后端成为唯一 `aria-pressed=true` 的分类，概念集合同步切换。
- 暗色状态：`docs/design/qa/homepage-star-map-dark.png`；1280×720。
- 移动端：`docs/design/qa/homepage-star-map-mobile-screen.png`；真实 390×844 iframe CSS 视口。应用内浏览器截图按 1.5 显示缩放得到 261×563 区域，随后归一化为 390×844；iframe 实测 `clientWidth=390`、`scrollWidth=390`、`clientHeight=844`、`scrollHeight=844`。

### Findings

未发现仍需处理的 P0、P1 或 P2。实现保留了参考图的横向技术分类、中心主题、无连线漂浮概念、暖骨白 / 苔藓绿 / 黄绿色北极星和大留白，同时把参考图右侧被裁切的概念收回安全区，保证真实页面无横向溢出。

### 五项保真检查

- 字体与层级：沿用项目系统中文字体；导航、分类、中心主题、概念四级字号清晰。中心标题在桌面 48–78px、移动端 35–48px，未出现截断或异常换行。
- 间距与布局：桌面 1280×720 的 `scrollWidth/scrollHeight` 与视口一致；390×844 也无横向或纵向溢出。分类数量继续由数据数组驱动，移动端使用横向滚动而非压缩全部标签。
- 色彩与令牌：只消费既有 `--bg`、`--ink`、`--muted`、`--accent`、`--brand-star`；浅色和暗色均保持足够对比，不引入蓝紫色、渐变、阴影或卡片表面。
- 图像与图标：品牌轨迹和所有星体继续使用项目真实 PNG 蒙版资产；没有用 emoji、字符星号、内联 SVG 或 CSS 图形替代。搜索与主题图标沿用既有站点组件。
- 文案与内容：首屏只保留品牌、技术分类、中心主题与概念名；旧 Hero、领域说明、列表解释、最近更新和 CTA 全部移除。

### 交互与无障碍

- 浏览器实际测试了分类切换、概念聚焦、中心术语详情链接、Escape 返回、搜索提交、明暗主题切换和移动端概念聚焦。
- 分类使用原生按钮与 `aria-pressed`，概念使用原生按钮，中心概念使用真实链接；状态变化通过 `aria-live` 播报。
- 桌面与移动端关键点击目标不小于 44px；键盘焦点有可见样式。
- `prefers-reduced-motion` 由样式回退与自动化断言覆盖；本轮应用内浏览器未暴露系统级动效偏好切换入口。
- 浏览器控制台日志为空。

### 比较历史

1. 初次实现为保证星图最小高度使用 610px，1280×720 下页面 `scrollHeight=854`，属于 P2 首屏比例漂移。将分类轨高度改为视口 clamp，并让星图高度取剩余视口空间；复验后 `scrollWidth=1280`、`scrollHeight=720`。
2. 概念按钮点击后原节点卸载，焦点回到 body，局部 `onKeyDown` 无法接收 Escape，属于 P2 键盘返回失效。改为窗口级 Escape 监听，保留组件卸载清理；复验中心态可恢复。
3. 移动端使用 390×844 iframe 实际渲染，复验分类横向浏览、星点不重叠、聚焦后仍为 `scrollWidth=390`、`scrollHeight=844`。

### Follow-up Polish

- P3：后续接入真实内容关系数据时，可用关联权重驱动星点距离与字号；当前使用固定的七个安全槽位，优先保证低噪与稳定排布。

final result: passed

---

## 历史：VibePolaris 首页 R3 设计 QA

## 对照范围

- 参考：苔藓编辑首页方案图，以及已选定的流星 / 北极星标识几何。
- 实装：`prototype/index.html`，1536×1024 默认浅色 `moss` 状态。
- 响应式：1536×1024、900×900、390×844。

## 结果

通过。当前实装保留参考方案的暖白纸张感、森林绿文字、黄绿色星体、发丝分隔线和技术领域横向编辑布局；按已确认反馈，把参考图中的“从技术领域开始”和大搜索框改成 VibePolaris 品牌首屏，导航搜索保留。流星图标使用真实透明资产并保持轨迹连续，星体比早期版本更大且与轨迹分色。

## 检查记录

| 优先级 | 检查项 | 结果 |
| --- | --- | --- |
| P0 | 页面可加载、脚本无语法错误 | 通过 |
| P0 | 领域切换始终只有一个展开内容 | 通过 |
| P0 | 390px 无横向溢出 | 通过 |
| P1 | 默认苔藓色与参考视觉一致 | 通过 |
| P1 | Logo 轨迹 / 星体资产、首入与 hover 动效 | 通过 |
| P1 | 明暗模式持久化、配色安全回退 | 通过 |
| P1 | 搜索查询页返回正确术语 | 通过 |
| P2 | 900px 双列与 390px 单列重排 | 通过 |
| P2 | `prefers-reduced-motion` 静态回退 | 通过（样式与自动化检查） |

未发现需要阻断交付的 P0、P1 或 P2 视觉问题。全页截图在应用内浏览器的拼接模式下偶发重复画面，DOM 节点数与普通视口截图均确认页面本身没有重复内容。

## BUG-21BCCBAD Logo 修复复验

### 证据与归一化

- 视觉问题来源：`docs/design/qa/logo-before-feedback.png`，112×81 像素，浅色 `moss` 静止状态。
- 修复后全视图：`docs/design/qa/logo-after-full.png`，1280×720 像素，对应 1280×720 CSS 视口。
- 修复后聚焦区域：`docs/design/qa/logo-after-focus.png`，190×165 像素，浅色 `moss` 静止状态。
- 两张聚焦图裁剪范围不同，因此不做逐像素重合判断；对照目标是用户明确指出的尺寸、终点对齐、星体层级和动画流畅度。

### 比较历史

**第一次反馈，结果 blocked。** P1：真实可视面积受透明素材边缘影响，首页标识虽然占位 74×64，但轨迹和星体明显偏小；星体中心落在轨迹终点左侧，视觉断开；轨迹使用 `clip-path` 逐帧揭示并强制同步布局重播，在蒙版图层上出现卡顿。

**修复。** 首页标识占位调整为 118×104，星体图层为 48×48；导航和页脚同步按比例增大。星体蒙版按透明边缘比例放大，中心重新落到轨迹终点；星体颜色从 `#A9C735` 提升到 `#BDD32F`。动画改为只使用 `translate3d`、`scale` 和 `opacity`，移除 `clip-path` 与强制读取 `offsetWidth`。

**第二次对照，当时结果 passed，后被真实观感反馈推翻。** 修复后轨迹可视宽高约为修复前的 1.7 倍，星体约为 3 倍；静止画面、1280px 与 390px 布局和控制台检查均通过。但该轮只验证了合成属性与少量静态状态，没有拆开中间运动帧，漏掉了 44% 与 74% 关键帧处的分段减速。

### 五项表面检查

- 字体与层级：wordmark 字体、字重和行高未受 Logo 放大影响；移动端 wordmark 缩至 40–52px，保持同一行。
- 间距与对齐：首页 Logo 与 wordmark 间距增至 38px，星体越界部分保留约 16px 视觉安全距；移动端 lockup 总宽 312px，未溢出 375px 内容区。
- 色彩：继续使用苔藓绿轨迹，星体提高黄绿色亮度但不引入蓝紫色或渐变。
- 资产质量：继续使用两张透明 PNG 蒙版资产；没有用 CSS 图形、字符或手绘 SVG 替代。
- 文案：无变化。

### 交互与无障碍

- 首入与悬停重播均使用合成属性；时长缩短为轨迹 520ms、星体 640ms。
- `prefers-reduced-motion: reduce` 下保持静态完整 Logo。
- 1280×720 桌面视口、390×844 移动视口与浏览器控制台已复验。

final result: passed

## BUG-21E057BE 首页降噪复验

### 视觉证据与状态归一化

- 用户标注源视觉：`docs/design/qa/homepage-density-user-annotation.png`，1923×1335 像素，红框指定需移除的导航副名、Hero 说明、领域说明、编号和长入口文案。
- 改动前浏览器源截图：`docs/design/qa/homepage-density-before.png`，1340×1218 像素，对应 1355×945 CSS 视口、密度已由浏览器归一化为 1×截图。
- 改动后浏览器实现：`docs/design/qa/homepage-density-after.png`，1340×1210 像素，同一 1355×945 CSS 视口、浅色 `moss`、AI · Agent 展开状态。
- 同屏全视图对照：`docs/design/qa/homepage-density-comparison.png`，2712×1274 像素；左侧为改动前，右侧为改动后。
- 额外响应式证据：`docs/design/qa/homepage-density-desktop-1536.png`，1520×1210 像素，对应 1536×960 CSS 视口；`docs/design/qa/homepage-density-mobile.png`，374×2307 像素，对应 390×844 CSS 视口。

### 比较历史

**用户标注时，结果 blocked。** P2：品牌定位在导航和 Hero 重复，领域上方辅助说明、“精选 01”类编号以及底部长 CTA 使页面扫读密度偏高。

**修复。** 移除上述重复文案，保留品牌主名、领域标题、短描述和主内容；五个领域入口视觉上仅显示箭头，同时保留 44×44px 点击区和完整 `aria-label`。

**修复后对照，结果 passed。** 同视口对照中，Hero 只剩单一品牌锁定，领域标题直接成为卡片首信息，入口箭头统一落在右下角。桌面 1536px、平板 900px 与手机 390px 的 `scrollWidth` 均不超过视口可用宽度；浏览器控制台无警告或错误。

### 五项表面与交互检查

- 字体与层级：字体家族、字重、字号与行高不变；删除信息后主标题与领域标题层级更明确。
- 间距与布局：Hero 高度保持 220px，仅改为单一锁定垂直居中；领域工具条缩至 64px，“查看全部领域”继续右对齐。
- 色彩与令牌：继续使用现有暖骨白、森林墨色、苔藓绿与黄绿星体，没有新增硬编码色或渐变。
- 图像质量：Logo 的真实 PNG 轨迹与星体资产、尺寸、锚点和 580ms 动画保持不变。
- 文案：红框指定的文案与编号全部不再出现；五个入口的可见文本均只为“→”，辅助技术名仍为完整“进入…章节”。
- 交互与可访问性：点击“前端”后仅前端变为 `aria-expanded=true`，再切回 AI · Agent 后仅该领域展开；主链接、搜索、主题和 Logo 动效未回退。

未发现可执行的 P0、P1 或 P2 问题。

final result: passed

## BUG-E64E8D8F 逐帧动效复验

### 视觉证据与状态归一化

- 帧序列目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，3×3 九个等时间状态，覆盖 0–580ms。
- 浏览器实现：`docs/design/qa/logo-motion-implementation-mid.png`，1265×712 像素，对应 1265×712 CSS 视口、1× 密度、浅色 `moss`、动画中段。
- 同屏聚焦对照：`docs/design/qa/logo-motion-comparison.png`，388×176 像素；左侧为 362ms 目标帧，右侧为浏览器约 350ms 的实际 Logo 区域。
- 两侧 Logo 均按 118×104 CSS px 归一化；聚焦图足以判断轨迹显现进度、星体位置、尺寸与衔接，不需要比较全页排版。

### 比较历史

**第三次用户实测，结果 blocked。** P1：星体只有 0%、44%、74%、100% 四个位置，并且相同 cubic-bezier 在每段重新起算，速度在两个中间关键帧附近明显收缩；轨迹整张蒙版平移淡入，和星体所在的曲线进度不同步。静态截图正确，但动态连续性不合格。

**逐帧修复。** 从批准的 `meteor-trail-mask.png` alpha 通道提取中心线，按等弧长生成 32 张累积轨迹帧，打包为 3776×104 的透明蒙版精灵图，控制在 4096px 纹理宽度以内以避免低端移动 GPU 分块。星体改用 9 个等弧长锚点和唯一的线性时间轴，在锚点间由浏览器连续插值；首入总时长统一为 580ms。导航悬停不再把星体瞬移回轨迹起点，只触发 360ms 原地轻闪。

**第四次对照，结果 passed。** 浏览器以约 64ms 间隔采样 10 次：轨迹蒙版位置依次为 0%、16.129%、32.258%、48.387%、64.516%、80.645%、96.774%、100%，星体矩阵在同一方向连续逼近 `(0,0)`，中间没有倒退、停驻或 easing 重启。362ms 目标帧与浏览器约 350ms 实现帧的轨迹长度、星体位置和尺度一致；桌面与 390×844 移动端均无横向溢出，控制台为空。

### 五项表面检查

- 字体与层级：未修改页面字体、字重、字号、行高或文案层级。
- 间距与布局：首页标识仍为 118×104，星体仍为 48×48；390px 端为 74×66 / 32×32，布局宽度 375px、scrollWidth 375px。
- 色彩：继续使用主题令牌中的苔藓绿轨迹与黄绿色星体，没有新增渐变或蓝紫色。
- 资产质量：轨迹 32 帧由批准的真实 PNG alpha 累积生成；星体继续使用原 PNG 蒙版。没有 CSS 图形、字符或手绘 SVG 替代。
- 文案：无变化。

### 交互与无障碍

- 首页首入使用 32 帧轨迹 + 9 锚点星体，统一 580ms。
- 导航 hover/focus 只播放 `vp-star-glint`，轨迹保持静止，避免重播时的瞬移跳变。
- `prefers-reduced-motion: reduce` 下取消两类动画，直接显示完整 Logo。

final result: passed

## BUG-722A6BD4 星体锚点复验

### 视觉证据与状态归一化

- 帧序列目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，浅色 `moss`、1× 密度。
- 桌面实现：`docs/design/qa/logo-motion-implementation-mid.png`，1265×712 像素，对应 1265×712 CSS 视口、动画约 350ms。
- 同屏聚焦对照：`docs/design/qa/logo-motion-comparison.png`，388×176 像素；左右均归一化到 118×104 标识槽位。
- 移动端实现：`docs/design/qa/logo-motion-mobile-after.png`，390×844 像素，对应 390×844 CSS 视口、完整静止状态。

### 比较与回归

- 用户反馈：星体在轨迹末端的位置偏左，需要轻微右移，不改变现有曲线、尺寸或动画节奏。
- 调整：118×104 首页标识中的 48×48 星体向右移动 4px；96×84 与 74×66 响应式标识分别右移 3px；导航与页脚小标识分别右移 2px 和 1px。
- 连续性：星体的 9 个动效锚点相对最终布局计算，因此所有中间帧与静止终点整体同步右移，没有首尾跳位。重新生成的同屏对照未发现轨迹断裂或星体越界。
- 五项表面检查：字体、字重、文案和主题色未变化；标识槽位与页面布局未变化；真实 PNG 蒙版保持清晰；仅星体横向定位发生调整。
- 浏览器回归：桌面 clientWidth / scrollWidth 为 1250 / 1250，移动端为 375 / 375；导航 hover 只触发 `vp-star-glint`，轨迹动画为 `none`；控制台为空。
- 结果：未发现可执行的 P0、P1 或 P2 问题。

final result: passed

## BUG-72B17ECF 左移与成长动效复验

### 视觉证据与状态归一化

- 逐帧目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，九个等时间状态覆盖 0–580ms，星体由 22% 增长到 100%。
- 浏览器逐帧证据：`docs/design/qa/logo-motion-implementation-145.png`、`logo-motion-implementation-290.png`、`logo-motion-implementation-435.png`，均为 1265×712 像素、1× 密度、浅色 `moss`。
- 同屏聚焦对照：`docs/design/qa/logo-growth-comparison.png`，388×528 像素；三行分别对照前段、中段和后段，左右 Logo 均裁成 180×140 像素区域。
- 静止响应式证据：桌面 `logo-motion-implementation-mid.png` 为 1265×712 像素；移动端 `logo-motion-mobile-after.png` 为 390×844 像素。

### 比较历史

**用户更正，前一轮通过结论被取代。** 星体不应向右微调，而应相对最初锚点向左移动 10px；原缩放在 25% 时已达 90%，视觉上缺少由小变大的过程。

**本轮修复。** 桌面首页星体从当前版本向左 14px，最终落在相对最初锚点左移 10px 的位置；中屏、移动端、导航和页脚同比例调整。九个缩放节点改为 22%、34%、48%、62%、75%、86%、94%、98%、100%，透明度从 35% 连续提升到 100%，仍与 9 个等弧长位置共享 580ms 线性时间轴。

**浏览器复验，结果 passed。** 以约 55ms 间隔连续采样，星体矩阵比例依次为 0.387、0.551、0.686、0.816、0.908、0.967、0.995、1.000，没有缩小、跳帧或 easing 重启。桌面最终定位为 `right: -12px`，移动端为 `right: -9px`；桌面 clientWidth / scrollWidth 为 1250 / 1250，移动端为 375 / 375。

### 五项表面与交互检查

- 字体与文案：未修改字体、字重、行高、字距或页面文案。
- 间距与布局：首页标识仍为 118×104，星体仍为 48×48；位置左移未改变品牌区占位，桌面和移动端均无横向溢出。
- 色彩与令牌：继续使用 `--brand-trail` 与 `--brand-star`，没有新增渐变、蓝紫色或硬编码业务色。
- 资产质量：继续使用真实透明 PNG 蒙版；三段同屏对照未发现模糊、遮罩断裂或透明边缘异常。
- 交互与无障碍：导航 hover 仍只触发 `vp-star-glint`，轨迹动画为 `none`；`prefers-reduced-motion` 静态回退未变；浏览器控制台为空。

未发现可执行的 P0、P1 或 P2 问题。

final result: passed

## BUG-88EB4284 再次左移与透明渐显复验

### 视觉证据与状态归一化

- 本轮源视觉真值：`docs/design/qa/logo-fade-before.png`，1265×712 像素、1265×712 CSS 视口、1× 密度、浅色 `moss`，记录修改前的静止锚点。
- 动效目标：`docs/design/qa/logo-motion-frameboard.png`，840×540 像素，3×3 九个等时间状态覆盖 0–580ms；首帧星体完全透明，随后同步移动、放大和显现。
- 浏览器实现：桌面 `docs/design/qa/logo-fade-after.png` 为 1265×712 像素，移动端 `docs/design/qa/logo-fade-mobile-after.png` 为 390×844 像素；两者均为动画完成后的静止状态。
- 聚焦同屏对照：`docs/design/qa/logo-fade-position-comparison.png`，388×196 像素；左侧为修改前锚点，右侧为修改后锚点，裁切范围和密度一致。
- 全页证据用于检查整体布局与溢出；聚焦对照用于判断星体相对轨迹末端和标题的水平位置。动效过程使用浏览器自然时间采样核验，避免用稳定化截图误判中间帧。

### 比较历史

**用户追加反馈，前一轮终点与透明度规格被更新。** 星体需在当前版本基础上再向左移动 10px；入场不能从 35% 不透明度开始，而应从完全透明逐步显现。

**本轮修复。** 桌面首页 48×48 星体从 `right: -12px` 调整为 `right: -2px`；40×40 与 32×32 响应式星体分别调整为 `right: -3px` 与 `right: -2px`，导航和页脚小标识同比例调整。九个透明度节点改为 0%、8%、22%、42%、64%、82%、94%、98.5%、100%，继续与 22%→100% 缩放和 9 个等弧长位置共用 580ms 线性时间轴。

**浏览器复验，结果 passed。** 动画自然时间采样的透明度依次为 0.007、0.068、0.188、0.359、0.504、0.695、0.847、0.946、0.977，缩放矩阵依次为 0.230、0.323、0.448、0.577、0.670、0.783、0.878、0.946、0.973；两组数值都单调增长，没有闪现、回退或重新起步。桌面最终为 `right: -2px`，移动端为 `right: -2px`；桌面 clientWidth / scrollWidth 为 1250 / 1250，移动端为 375 / 375。导航 hover 仍只触发 `vp-star-glint`，轨迹动画为 `none`，控制台为空。

### 五项表面与交互检查

- 字体与文案：未修改字体、字重、字号、行高、字距或页面文案。
- 间距与布局：品牌标识占位和星体尺寸不变，仅星体水平锚点改变；桌面与移动端均无横向溢出。
- 色彩与令牌：继续使用 `--brand-trail` 与 `--brand-star`；透明渐显只改变不透明度，不引入渐变或额外颜色。
- 资产质量：轨迹与星体继续使用真实透明 PNG 蒙版；对照图未发现遮罩断裂、模糊或透明边缘光晕。
- 交互与无障碍：首入动画保持 580ms，hover/focus 与 `prefers-reduced-motion` 回退规则未变；核心导航和领域展开行为未受影响。

未发现可执行的 P0、P1 或 P2 问题。

final result: passed
