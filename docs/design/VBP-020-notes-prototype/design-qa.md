# VBP-020 原型局部验收

日期：2026-09-21。范围：独立本地原型，非正式站点发布验收。

final result: passed

## 视觉来源与捕获

- 来源：`reference/selected-margin-notes.png`，用户选择的第 1 张页边批注设计稿。
- 最新用户要求覆盖原稿第二条列表排布：批注对应正文同一行，随原文滚动。
- 桌面：1487 × 1058 CSS px，截图同尺寸；手机：390 × 844 CSS px。
- `qa/comparison-before.jpg`：原稿与初版同尺寸并排。初版仍把第二条笔记堆在首条下面。
- `qa/comparison-final.jpg`：原稿与最终实现同尺寸并排。
- `qa/annotation-comparison.jpg`：同位置裁切，检查正文、批注和连线。
- `qa/desktop-final.png`：首处批注与正文同行。
- `qa/line-aligned-context.png`：滚动至下一节后，第二条批注位于第二处原文旁。
- `qa/collision.png`：同一行两条批注按原文顺序排列；第二条向下避让。
- `qa/mobile-notes.png`：窄屏底部面板。

## 迭代与判断

1. 初版 P2：示意图工具说明出现孤字换行，正文字号与标题节奏偏离原稿。已调整网格、图标及字号，重新截图对照。
2. 用户修订 P1：批注堆成列表，无法直接对应原文。已按 Range 首行实时定位，移除固定列表滚动，独立随手记改在笔记库编辑。
3. 相邻批注检查发现同一行的卡片可能受编辑态内边距影响排序。已改用原文纵坐标稳定排序，保持同一行的原文先后顺序。复查首条偏移 0px，后条避让 151px，卡片间无重叠。
4. 最终：布局、品牌色、阅读层级与选定方向一致，修订后的同行关系成立。无未解决 P0/P1/P2 视觉问题。

保留差异：批注标题置于内容上方，使批注内容首行对齐原文；第二条位于其实际出处，不出现在第一页。预置笔记显示“示例笔记”，真实写入显示“已保存在此浏览器”。图标使用现有品牌资源及 Phosphor，未复刻生成图中的不规则笔画。系统字体及字形有轻微差异。

## 已执行验证

- `npm run build`：通过，生成前端构建与原型模板约定的 worker 输出。
- `node --test tests/notes-data.test.mjs`：2 项通过，覆盖锚点漂移/歧义和损坏备份校验。
- 真实鼠标选择正文 → 写笔记 → 自动保存 → 刷新：笔记与高亮保留。
- 我的笔记搜索、删除、撤销、返回原文：通过。
- 点击导出获得下载及页面反馈；导入本地 JSON 夹具后记录出现，已有记录保留。未将导出文件再导入作完整往返验证。
- 桌面两处批注随对应正文滚动；调整视口后重新定位；同一行两条批注不重叠。
- 390px 手机打开/收起批注面板，创建随手记、键盘输入与失焦保存：通过。文档宽度 390px，视口宽度 390px，无横向溢出。
- 浏览器捕获的 error/warn 日志为空。测试生成的数据已清理，仅留下标记明确的初始示例。

## 实际边界

存储失败处理和草稿保留已实现，但未注入浏览器容量耗尽或禁用存储故障；未做跨浏览器、多标签并发和真实手机软键盘矩阵。没有运行无关全站回归或发布检查。正式网站开发、dev 集成与生产部署不在本轮范围。

## DP 记录状态

原型研发任务已完成，需求 VBP-020 保持规划中。测试计划已创建，执行结果写入被 DP 状态门禁拒绝：`REQUIREMENT_NOT_IN_TESTING`（只有测试中或已交付的需求可执行计划）。没有为了原型验证把正式需求推进到测试中；计划保持 ready、执行数为 0。实际本地验证证据保存在本文，并在需求描述登记。配置的 Obsidian 库在此机器不存在，未同步。

## 2026-09-21 批注图标与时间修订

按用户附图反馈，把正文边上的笔记图标移到原“我的想法”位置；移除该标题以及批注底部的常规保存/来源状态文字。时间改为本地 24 小时制 HH:mm，鼠标悬停或键盘聚焦时在左侧显示完整年月日。保存失败的重试入口保留。

本轮 `npm run build` 通过。真实浏览器在 1117 × 803 视口检查默认与悬停状态：图标在批注上方、正文仍同行、无常规保存文案；时间显示 12:48，悬停显示 2026年9月21日，日期右缘距时间左缘 12px；移出键盘焦点而鼠标仍悬停时日期继续可见。截图为 `qa/note-chrome-default.png` 与 `qa/note-chrome-hover.png`。未修改用户已有笔记内容，没有重复执行上一轮存储/备份流程；既有 DP 门禁仍适用。
