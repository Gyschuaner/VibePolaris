# VBP-018 · 请求输入的三个位置

第006批，按长期目标每批三页推进，基线dev 4d5eb2d。原有三页均为通用自动节点图、测验与提示词。保留关系数据和六个旧锚点，换成可独立阅读的文章。

## 机制差异与场景契约

| 词条 | 首图构图 | 主体操作与证据 | 阅读组织 |
| --- | --- | --- | --- |
| 查询参数 | 六本书被筛选，保留其中两本 | 编辑原始query或选择样本；原生URLSearchParams给出get/getAll，按本例tag/q规则筛选真实数组；不命中的书淡出，编辑撤销旧结果 | 从地址中的条件到集合筛选，重复键/空值对照，再讲编码与共享边界 |
| 路径参数 | 可替换槽位接入42，定位读者 | 路径进入静态/动态两条有序路由；交换顺序，显示捕获值，再做编号、权限和存在性检查；只有200能显示资料 | 模板与实际值并置，匹配台居中，校验与授权常驻，框架规则补充 |
| 请求体 | 表单折合成JSON内容 | 输入书名与数量，选择JSON/表单编码；先实际序列化，再按Content-Type解析并校验；415/400/422不生成接收对象 | 字段/编码/接收工作台，格式选择短对照，语法和业务的分层解释 |

所有场景在本地执行，不调用真实服务。路由器只实现文中两个模式，接收端只实现文中两种格式，不冒充通用框架。输入或规则变化立即使旧证据失效；重置回初态。复用States/Reveal及ConceptHero生命周期，退出保持DOM到过渡完成；无新依赖、定时器或常驻帧循环。

## 已读资料与写作边界

2026-09-21实际阅读：RFC3986 §3.3/3.4；MDN URLSearchParams（get/getAll、set/append、编码和空值）；OpenAPI3.1.1 Path Templating/Paths Object；FastAPI Path Parameters（类型转换、校验、声明顺序）；OWASP Authorization/Input Validation及query信息泄露；MDN Using Fetch、Using FormData、Content-Type、415。

OpenAPI文档约定与FastAPI执行顺序分开写；示例422/404/403是本站教学接口约定。query常见键值编码不是URI唯一合法语法；空值、缺失和重复键不混写。请求体不等于JSON；multipart由浏览器生成boundary，正文解释但本批不实现文件上传。来源精确URL、段落映射与可确认日期在lib/request-input-sources.ts。所有书目必须有正文对应，不凑来源数量。

## 验收范围

先build；一项断言验证三套真实机制；三页桌面和390px受影响流程、失败分支、重置、键盘及开合中间态；首图终态、主题变量与引用回链；18个旧锚点。未改的公共星图不扩大回归。完成后补实际证据；不将代码审查冒称浏览器通过。

## 集成范围

功能分支feat/VBP-018-request-inputs，PR合入dev后重启本地3001并做限定集成检查。远端dev环境未配置，不发布main/生产。备份基线4d5eb2d17f24b0403312751f78fe4f41d8adff5d；必要时独立目录检出、构建、重启3001，不覆盖工作区。当前Mac无D:/Obsidian/gysnote，跳过知识库同步。

## 实际验收与 review

- 构建通过，生成312个静态页面、301个词条；一项机制断言通过，覆盖重复键/空值/特殊字符、路由顺序/拒绝/不存在、JSON和表单往返及415/400/422。
- 1280px真实浏览器：查询科学2本、科学或艺术4本、逗号单值0本、空值0本、无tag6本、q=星空1本；编辑立即撤销筛选。编码“海+风 & 书”再解析得到原文。Enter可应用，重置回初态。
- 路径42定位林舟；me先匹配固定路由，交换顺序后捕获me并422；abc同样422。43无权限403且没有资料，允许后200定位许青；99允许后404。顺序交换存在0.5秒移动过渡，中间两张卡片位置变化；重置恢复路径、顺序与未处理状态。
- 请求体JSON序列化为43字节，接收200；名额0得到422；错标text/plain得到415；去掉花括号得到400；表单编码特殊字符仍还原原值并200。编辑禁用接收、撤销旧结果，重置恢复JSON/2名额。编码和接收均通过键盘Enter实际操作。
- 三页390px实际操作无横向溢出：查询四本匹配与编码长文本、路径编码斜杠拒绝、请求体重置后重新编码接收。桌面与窄屏结果画面已检查，窄屏使用正常字号和纵向排列。
- 引用展开读取实际段落摘录；书目返回body-stream时URL定位正确，并自动打开补充内容，目标未被页头遮挡。补充开合使用原生details-content过渡，实际采到开启中间态height157.75px、opacity0.95，持续400ms。18处角标目标与18个历史锚点完整，无重复ID。
- 三页通读核对：无需操作也能读到定义、过程和边界。规范、框架行为和本站简化规则分别说明；18个论断对应11条书目。首图分别呈现集合筛选、槽位定位、内容编码，主操作也不同。Review将筛选首图保留的位置改为第一、第三本，与正文的科学分类一致。
- 复用States/Reveal持久层及ConceptHero离屏/后台暂停，没有新定时器、循环或依赖。减少动态效果终态与主题变量做代码审查；本批未改系统动态设置或切换主题，不写成实机通过。未改的公共星图不扩大回归。

DP已创建VBP-018、任务与3项测试计划，需求在开发中。任务状态更新及后续查询连续TLS握手超时；doctor仅确认本机CLI/凭据存储可用。真实验收证据先留在本文，恢复连接后补测试执行和阶段状态，不将本地通过冒称DP已同步。计划ID efc3d7e0-edf9-4911-b65b-c57fec2e0f5d。
