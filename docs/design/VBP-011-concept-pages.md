# VBP-011：五个概念页

以现有 Harness 阅读风格扩展五页。保留正文、来源、局部名词卡片和关联星图，不把所有概念套成同一条流程。

| 词条 | 核心机制 | 首图 | 主体交互与阅读编排 |
| --- | --- | --- | --- |
| 记忆 | 保存后还要取回 | 进度纸进入档案、会话接手 | 会话与外部记录分区；手动保存、新会话、取回、删除；交接单 |
| 上下文窗口 | 输入与输出共享有限空间 | 容量条分配变化 | 原生滑杆、固定上限与摘要；材料清单、压缩前后对照 |
| 提示词 | 材料、结果与边界形成任务要求 | 模糊请求被批注改写 | 可增删的任务稿与固定回答；旁注、格式样例、核对步骤 |
| MCP | 连接、发现、调用各自有作用 | 插接与能力出现 | 应用/服务器拓扑、工具注册区、请求记录；角色图和能力列表 |
| 执行沙箱 | 运行环境限制访问范围 | 操作抵达限制边界 | 操作对象在内外区域移动，目录和网络分别约束；权限对照 |

## 资料与适用范围

2026-09-21 核对公开一手资料。完整 URL、机构、可确认日期及段落锚点以 `lib/extended-concept-sources.ts` 为准，不手工复制正文摘录。

- LangGraph Memory overview：会话范围、跨会话存储、检索及维护；框架术语注明出处。
- Anthropic 长任务文章：进度文件和 Git 的交接实践，不写成所有记忆系统的唯一实现。
- Claude Context windows：输入/输出/工具及缓存的计数边界；不固化模型规格。100 格是教学比例。
- Anthropic Context engineering：选择、摘要和外部记录；摘要并非无损扩大窗口。
- Google Prompt design、Claude Prompting best practices：具体要求、材料组织、样例及模型差异；本页不是在线模型实验。
- MCP 官方 2026-07-28 版 architecture/server concepts：角色、传输、tools/list、tools/call 和三种能力。省略完整报文，不沿用旧版本初始化握手假设。
- Anthropic sandboxing、gVisor 文档：环境隔离与实现差异；限制范围不等于绝对安全或任务正确。

## 验收范围

仅覆盖五页的新交互、引用与布局；复用组件不触发全站回归。构建后，真实浏览器检查桌面和 390px，重点看保存/取回分离、预算溢出/压缩、旧回答失效、MCP 顺序/断开/离屏暂停、沙箱拒绝/网络规则。检查新增来源锚点与星图中心。实际结果记录到 VBP-011 关联计划。

开发集成通过 PR 进入 dev；当前仅配置本机生产构建预览。未授权 main/生产发布。指定 D:/Obsidian/gysnote 在当前 Mac 不存在，跳过同步。

## 本次验证结果

- `npm run build` 通过：312 个静态页面生成、TypeScript 通过。无新增依赖。
- 浏览器：1109px 桌面与390px窄屏完成五个新演示的上述路径；MCP 另检查1440px侧标题。五页窄屏文档宽度均为390px；临时视口已恢复。
- 引用：五页书目展开得到对应正文；沙箱 gVisor 摘录返回 `#sandbox-implementation` 并聚焦。保留原页面 question/definition/scene/quiz/prompt/learning 的锚点入口，旧 quiz 链接转到边界解释，不恢复旧题目模块。
- 修复并回归 BUG-9792C4A2（沙箱停靠、提示词缺材料与旧结果）和 BUG-F3C7F2B6（大屏标题单字换行）。
- 动态：MCP 离屏停止自动播放；首图共用可见性/页面隐藏控制。减少动态效果的完整终态已检查 CSS，未改动用户操作系统偏好做实机切换。
- 结果记录在 DP 测试计划 `404823cc-33ed-4d15-a20f-8c5c706189aa`。测试为固定教学交互，不代表真实模型或真实沙箱/MCP服务经过验证。
