# VBP-008 搜索与分类工具栏

2026-09-21，按用户本轮明确反馈调整首页：全部、前端、后端、AI · Agent靠左紧凑排列；搜索由右侧44px入口平滑展开为输入框，图标随框左边缘移至输入区左侧。桌面330px、手机视口减28px，保持原来的分类与画布尺寸，不触发布局挤压。输入框沿用surface、ink、muted与hairline主题变量，不再固定白底。

复用原生popover，350ms宽度缓动与200ms透明度变化；输入内容稍后淡入。Escape、外部点击、自动聚焦、查询参数定位与结果选择保留。手机搜索展开时淡出并隐藏被覆盖的分类，关闭后恢复；搜索图标入口只淡出，以保留原生焦点返回。减少动态偏好关闭这些过渡，无新增依赖。

## 验证

- 最终npm run build通过，49静态页面；未运行全套check或扩大为全站回归。
- 1280×720：分类占x40至322.04，间距28px。搜索框从44px，经316.76px，到330px；关闭时从330px，经48.77px，再隐藏。真实中间值与布局数据见measurements.json。
- 搜索展开前后画布均为x0、y145、1280×575；搜索图标x938、输入框x968，图标在输入内容左边。
- 后端领域输入API并按回车，定位API接口概念卡，URL保持cat并加入term=api；Escape和点击分类关闭正常。
- 390×844：四个分类占x18至238.52，均完整显示；搜索框x14、宽362，无水平溢出；无匹配词显示空结果。截图见desktop-closed/open和mobile-closed/open。
- BUG-641A4FEB：键盘检查发现对搜索入口设置visibility:hidden会影响原生焦点返回；修复为仅淡出入口，重新构建并回归，Escape后焦点返回“打开概念搜索”。手机被覆盖分类仍使用visibility隔离。
- 浏览器error/warn为空；临时视口已清除。未进行跨浏览器或真机软键盘回归；减少动态分支为代码核对。

DP实现任务eeb304ea-edc1-4931-9e68-8496b6f35071；用例1466fa3b-21f4-49e2-8584-4f237e470752；计划7fcae472-bbb4-4fa9-b645-0d713d15cd9f。分支feat/VBP-008-compact-search-toolbar。实际测试执行、合并和部署提交以DP记录为准。本轮不扩大处理需求原有的其他待回归Bug。

集成目标为本地dev http://127.0.0.1:3001/ ，远端dev未配置，未部署生产。回滚基线90837aa371e7e78cd4d414edc4c7fe997c213ae4：独立目录检出、构建并重启3001，不清除用户浏览器数据。指定Obsidian库D:/Obsidian/gysnote在本机不存在，跳过；记录保存在Git和DP，未新建飞书正式文档。
