# VBP-021 阅读侧栏与模块精简

桌面目录／批注栏共用右上角的收起按钮，收起后保留一个展开按钮。正文阅读列与右侧310px区域保持固定，开合不改变列宽、间距或正文结构；只对侧栏内容做18px位移、360ms缓入缓出及淡入淡出。连线等侧栏滑入完成、重新测量终点后出现；减少动态偏好下直接切换。复用现有组件与CSS，不增加依赖。

收起偏好使用当前浏览器的 `vp-reading-sidebar-collapsed`。存储不可用时仍可操作。创建批注、点击正文高亮或定位笔记会展开；收起不卸载编辑器，也不清理IndexedDB笔记。手机仍使用原有底部面板。

选型指南与工具模块暂时下线：移除导航、页脚、关于页及站点地图入口，`/guides`和`/tools`临时跳回首页。HTML、CSS、JavaScript深度教程保留，面包屑改为对应词条。

BUG-8D21A94F修正原先开合时正文被拉宽／挤窄的问题。`npm run build`通过；真实鼠标开合时Harness正文宽834px、段落宽740px高99.45px、演示宽760px以及scrollY均保持不变；侧栏中间位移1.63px／16.38px，保留缓动。已有4条批注恢复后连线终点横向偏差均为0；关闭时连线隐藏。CSS教程开合前后正文坐标、宽846px及高度一致，无水平溢出和相关error/warn。未扩展跨浏览器、存储故障或系统减少动态设置实测。

上一版已验证偏好刷新恢复、键盘展开、写批注、390×844底部面板以及旧模块地址跳转；本次不更改这些流程。修复用例 `c7f19885-af82-41a4-bfd6-16df8bc530db`，计划 `0f3139aa-a4f0-45e5-a9d4-55dc2d9b07fd`。

DP任务 `1ef8db4c-28b2-49db-9d2c-496b0ca562c0`；用例 `125622ef-f4ce-4fd2-a635-fa8e536a552a`；计划 `2a4936b0-429c-4a8d-8778-07b539bceb67`。功能分支 `feat/VBP-021-reader-sidebar`，实际合并和部署提交以DP记录为准。

集成目标为本地dev `http://127.0.0.1:3001/`；远端dev未配置，生产未授权。BUG-8D21A94F功能分支 `fix/BUG-8D21A94F-stable-reader`，本次回滚基线 `0894fae71b7f8218bc9f9cf5363d6ff8cc3c9188`：在独立目录检出、构建并重启3001，不清理浏览器笔记。Obsidian配置路径 `D:/Obsidian/gysnote` 在本机不存在，本次跳过，研发记录保存在本文及DP。

## 方案2：简约侧栏

用户选中第二张设计稿，参考与证据保存在 `VBP-021-soft-sidebar/`。顶部改为浅底分段切换，灰色记一条与NotePencil图标置于第二行右侧；移除长竖线、页签下划线及横线，收起按钮仍在右上角。分段底色使用现有中性色变量，激活项使用surface和accent-text。清除Harness目录旧64px上边距，保留正文网格、已有星星及批注布局。

功能分支 `feat/VBP-021-soft-sidebar`。DP实现任务 `8fa7121c-1437-4efb-9a8a-94453b6e18cc`，用例 `9336321f-34fd-4203-a666-1be3a40933f2`，计划 `40849529-8b22-4103-bea2-c3843dde27b9`。构建与本地3002视觉、鼠标/键盘开合、笔记入口、4条连线及390px底部面板检查通过，详见根目录design-qa.md；实际合并提交和3001部署状态以DP记录为准。

本次回滚基线 `fe6f8fb48dd9bc9ba96b387bdd2b858c9a9d0546`：独立目录检出该提交，构建并重启3001，不清理本地笔记。未部署生产。Obsidian配置路径在本机仍不存在，跳过；未产生新的飞书正式文档。
