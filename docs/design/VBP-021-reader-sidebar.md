# VBP-021 阅读侧栏与模块精简

桌面目录／批注栏共用右上角的收起按钮，收起后保留一个展开按钮，网格侧栏由310px变成40px。按用户补充，宽度与间距使用360ms缓入缓出，内容与批注连线淡入淡出；减少动态偏好下直接切换。复用现有组件与CSS，不增加依赖。

收起偏好使用当前浏览器的 `vp-reading-sidebar-collapsed`。存储不可用时仍可操作。创建批注、点击正文高亮或定位笔记会展开；收起不卸载编辑器，也不清理IndexedDB笔记。手机仍使用原有底部面板。

选型指南与工具模块暂时下线：移除导航、页脚、关于页及站点地图入口，`/guides`和`/tools`临时跳回首页。HTML、CSS、JavaScript深度教程保留，面包屑改为对应词条。

验证：`npm run build`通过。浏览器确认收起与刷新保留、Enter展开、动画中间列宽处于40–310px之间；收起后选字写笔记可展开，编辑内容保留，批注连线恢复。390×844底部面板可开合，无横向溢出；旧模块地址均回首页，CSS教程正常，浏览器无error/warn。未扩展跨浏览器、禁用存储及减少动态系统设置实测。

DP任务 `1ef8db4c-28b2-49db-9d2c-496b0ca562c0`；用例 `125622ef-f4ce-4fd2-a635-fa8e536a552a`；计划 `2a4936b0-429c-4a8d-8778-07b539bceb67`。功能分支 `feat/VBP-021-reader-sidebar`，实际合并和部署提交以DP记录为准。

集成目标为本地dev `http://127.0.0.1:3001/`；远端dev未配置，生产未授权。回滚基线 `c99665297657aa7fca13930b628dadc07dce90a7`：在独立目录检出、构建并重启3001，不清理浏览器笔记。Obsidian配置路径 `D:/Obsidian/gysnote` 在本机不存在，本次跳过，研发记录保存在本文及DP。
