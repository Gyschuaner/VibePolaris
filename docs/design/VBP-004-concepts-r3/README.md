# VBP-004 · 三版桌面原型 R3

2026-09-06。用户再次否定当前 Harness 页面，明确要求先做三版原型图与详细方案。本轮仅设计探索，不修改业务代码；当前 R2 运行页面不是新方案的实现。

正式方案：[VBP-004 桌面 Harness 词条设计方案与三版原型 R3](https://ycn7t34xe864.feishu.cn/docx/VWilddQlHoNCEqxlGNjcEwMnn5f)。包含三图、推荐与取舍、首页连续性、桌面布局尺寸、约 17 秒动画分镜、交互与异常状态、验证标准、生成图问题及实施顺序。

## 本轮图片映射

编号严格对应本轮聊天中实际展示顺序，尚无用户选定方案。

| 编号 | 方向 | 原型文件 | 核心差异 |
| --- | --- | --- | --- |
| 1 | 星图剧场 | 01-star-theatre.png | 模型与工具构成开放星图，失败回程作为主动画 |
| 2 | 任务画布 | 02-task-canvas.png | 网页产物占据主画面，直接看到修改与修复 |
| 3 | 接入实验 | 03-harness-switch.png | 一个开关接入运行环境，同一模型与任务对照 |

建议优先考虑 1，但必须由用户选择。首页继续使用现有星图与选中特效；三版均针对 Harness 词条页。共同任务为“给这个页面加上深色模式”，用于直接展示执行结果，替代此前纯文字反馈归类。

## 自检与收敛要求

- 图 1：加大结果预览，去掉重阴影与仿浏览器彩色圆点；检查失败须在产物中定位真实问题，不能只改路径文字。
- 图 2：统一“正在修复 / 已修复”状态；保留一个重播入口，避免误解为主题编辑器。
- 图 3：移除下方重复开关与多余完成状态；保留检查回到模型的路径，避免误解为单向工具流水线。
- 三图均有生成式图像的状态细节偏差，适合方向评审，不能直接作为逐像素最终验收稿。

使用内置 image_gen，恰好三次独立生成，每次均附当前词条截图 reference-current.png 和既有星图 ../VBP-004-desktop-r1/existing-motion/01-idle.png。目标尺寸 1440×1024；实际图片均为 1487×1058，未拉伸。完整提示与来源在 generation-prompts.json。图像已逐张目视检查，不声称静态图验证了动画或理解效果。

## 实施交接边界

选定方向后先收敛状态与控制数量，再实现四种产物状态：初始亮色、首稿低对比度、定向修复、验收停止。之后接入工具返回和失败回路动画，最后连接首页过渡及阅读内容。实现必须让产物状态驱动检查结果，不用预设成功文字代替验收。最多 3 次尝试，权限拒绝与预算耗尽均停止；减少动态时仍可逐步查看结果。

本轮没有启动新实现、运行新功能测试或 dev/生产部署。只检查图片数量、尺寸、主要中文内容、三版差异、已知问题及文档完整性。VBP-004 为 in_development，设计任务 59f9c2ff-7929-436b-86d2-57ee4b82308f 已完成交付；BUG-F319515E 保留 ready_for_retest，用户尚未确认设计改善。原有工作区三项用户文件未修改。

Git 分支：feat/VBP-004-desktop-harness；本轮提交仅包括本目录的设计材料，无 dev/main 合并。
