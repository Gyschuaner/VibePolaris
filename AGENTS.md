# VibePolaris 项目协作规则

## 任务范围与执行

按用户目标完成已授权工作；常规实现细节自行判断，只有会实质改变需求、造成数据破坏、涉及权限边界或无法安全判断的问题才询问。需要用户批准时，先完成独立且已授权的准备工作，提供可审阅结果。用户最新明确要求优先于旧的本地规则与 Skill 指南，系统和开发者约束仍适用。

这些规则服务于本次实际任务。产品研发按下述 DP、Git 和发布流程执行；纯个人配置、Skill 或指令维护按其范围交付，不自动扩展为产品功能、全仓审计或发布任务。仅维护有后续价值的记录。

编写、修改、重构或审查代码时，必须读取并执行已安装的 `ponytail` Skill，默认 `full`；当前机器路径为 `C:/Users/chuansgu/.codex/skills/ponytail/SKILL.md`。同一上下文已读取时无需重复。遵从用户指定的等级或停用要求；纯文档、检索和问答不自动触发。优先复用现有实现、标准库、平台能力和已有依赖，以完整满足需求为准，不为缩短代码牺牲正确性、可读性、安全或数据保护。

## 事实来源与工具

- DP 是项目研发管理的唯一事实来源。项目固定为 **VibePolaris（Vibe 指北）**，key 为 **VBP**，ID 为 `8c243d4c-cf5e-4eca-a682-0a12cc0660b0`。需求、研发任务、负责人、Bug、实际测试、版本发布、研发进度和文档关联记录在 DP。
- 使用 `developer-platform-cli` Skill 和 `dp.exe`。优先解析 `DP_CLI_PATH`，再查 PATH；正常情况下复用该路径。业务命令统一带 `--project 8c243d4c-cf5e-4eca-a682-0a12cc0660b0`，不以 `dp project use` 切换全局状态。CLI 已支持的能力不得绕过 CLI 调 API、操作数据库或复用浏览器 Cookie。
- 飞书承载产品方案、技术设计、调研、测试报告、发布说明和复盘等正式文档，按需使用相应 `lark-*` Skill 与 `lark-cli`。DP 保存文档索引和关联，不复制飞书正文。Git 中的 `docs` 保存与代码紧密相关、需要版本控制的文档和必要研发、部署记录。
- Git 是代码和配置的唯一事实来源，仓库为 https://github.com/Gyschuaner/VibePolaris 。保护用户已有未提交修改；所有提交都须追溯到 DP 需求或 Bug 编号。

## 开始工作时

读取适用的 AGENTS.md，检查 Git 分支、工作区和相关远端同步状态。首次需要项目概览或开发命令时阅读 README；根据改动读取相关技术文档，涉及部署时再读部署说明。上下文已足够且文件未变化时不重复读取，不以完整仓库地图或整套文档作为每次编辑的前置条件。

用户提供 DP 编号时，查询当前状态、验收标准、负责人及参与人、研发任务、关联文档、测试计划和发布版本；按本次涉及的部分继续。新功能先搜索重复需求，无重复项时补全标题、背景、目标、范围、验收标准和优先级并创建需求。缺陷修复关联或创建 Bug，记录复现方式、预期结果和实际结果。

开始产品实现时建立或更新研发任务，设置总负责人及实际涉及的阶段负责人，同步真实状态。普通问题按已有上下文合理补全；关键缺口才向用户提问。

`dp --version`、`dp doctor --json`、`dp auth status --json` 仅用于首次安装、环境异常或命令报错诊断。

## Git 与发布

- `main` 对应生产，`dev` 对应开发集成环境；两者是保护主干，不得直接 push，改动通过功能分支合并。
- 产品实现从 `dev` 切出带 DP 编号的分支：新功能用 `feat/VBP-001-xxx`，修复用 `fix/BUG-001-xxx`；`chore/`、`docs/`、`refactor/` 按实际用途命名并包含对应编号。Skill 示例中的默认分支不能覆盖本项目的分支模型。
- 交付产品代码时，完成本次要求的本地验证后合并 `dev`；涉及运行行为的代码集成需部署 dev 并完成与改动相关的集成验证。这是进入相应集成阶段的要求，不是每次本地编辑或指令维护的前置条件。
- **合并 main 或部署生产需要用户明确授权。** 用户已说“完成后上线”“测试通过后直接部署”等，即授权相应完整发布流程，无须重复询问。完成必要验证后由 dev 合并 main、部署生产、执行健康检查及核心功能冒烟，并记录版本、提交号、备份和回滚方法。
- 本地修改、Git 提交、dev 集成和生产上线分别如实报告；未发生的阶段不得声称完成。

## DP 状态与写操作

- 写操作前查询相关对象，使用当前版本执行修改，完成后重新查询验证。保留本次有效查询结果；不因进入另一步骤就重查所有无关对象。
- 状态变化前读取 `allowed_transitions`；不绕过权限、版本冲突、状态流转和发布条件。
- 遇到 409，重新读取对象，确认原意仍成立后再重试；401 需要重新登录，403 是权限不足，不能当作登录失败处理。
- 供程序读取的输出使用 `--json`；参数和状态值不确定时查命令帮助，不猜测。

## 测试与质量

【避免过度测试】
优先从代码实现本身解决问题，选择代码层面的最优方案，尽量一次性完成。
不要为了显得验证充分而编写大量低价值测试用例，也不要反复运行已经通过、无法带来新信息的测试。
先确保受影响代码能够编译或构建通过。只有任务明确要求、仓库规则强制规定，或出现具体失败信号时，才增加必要测试。
编译或构建通过后，没有具体失败信号就停止。
如果当前环境确实无法确认某些部分，交付时说明未验证项和原因，并给出最小手动验证步骤、预期结果和注意事项。需要继续测试时，先说明测试思路和范围，等用户决定，不要自行扩大测试。

这里的“停止”指停止追加验证，已明确要求的检查和其余已授权交付工作仍需完成。构建通过不代表运行时行为或用户验收已通过。

- 普通代码改动先使用 `npm run build`；`npm run check` 包含测试、静态检查和构建，仅在本次验收或发布要求需要整套检查时执行。纯文档、Skill 或指令变更未影响构建输入时，仅检查内容、格式和引用，不运行无关应用构建。
- 验收标准按实际改动考虑正常流程、边界、权限或状态限制、失败处理及关键回归；只覆盖确实相关的项，不把每个分类自动转为新增测试用例。
- 前端交互验收和影响运行行为的 dev 集成需要真实浏览器操作，范围限于本次受影响流程；浏览器不代替 dp CLI 操作业务记录。纯文案或指令变更不触发整站浏览器回归。
- 实际执行或明确要求的测试用例、计划及结果记录在 DP 并关联需求；测试发现缺陷时关联或创建 Bug，修复后记录相应回归证据。没有测试任务时不创建空计划或虚构执行结果。
- 超出既定范围的继续测试，先说明要回答的问题、测试方法和范围，等用户决定。未能执行时说明阻塞原因、已有证据、剩余风险及最小手动验证方式，不声称未运行的测试通过。

## 飞书与 DP 文档关联

为某个需求操作飞书文档时，取得文档标题、规范化 URL 和明确的 DP 需求编号；Wiki 链接先用 lark-cli 识别底层真实文档。不要根据标题或主题相似度猜测关联。

完成飞书操作后关联索引，并重新查询需求确认关联：

```text
dp --project 8c243d4c-cf5e-4eca-a682-0a12cc0660b0 document ensure --url <飞书文档URL> --title <文档标题> --requirement <需求编号> --json
```

文档创建成功而 DP 关联失败时，保留文档，只重试 `document ensure`，不得重复创建。更新关联时保留原有人工标题、摘要、分类、标签及其他关联。访问令牌、DP PAT、密码等不得写入命令参数、代码、日志或文档。

## 记录与交付

每次实现、排查或重要调整后，更新 Obsidian `gysnote/项目训练/VibePolaris` 中有用的研发记录。当前机器的库位于 `D:/Obsidian/gysnote`；若不存在，跳过并说明。

按本次实际工作更新相关 DP 对象、飞书文档及索引、Git 提交、发布和回滚记录；不为满足形式创建空文档。只有产生或修改正式方案、验收或发布内容时才维护相应飞书正文；本地指令维护可直接记录在 AGENTS.md 与知识库。

完成以本次范围和验收标准为准：实现符合要求，必要检查有证据，相关记录与真实状态一致。产品代码进入 dev 或生产时须完成上述对应阶段要求；环境阻塞和未执行阶段明确列出。简洁汇报实际涉及的 DP 编号及状态、主要改动、分支和提交、验证结果、文档关联、部署结果及后续事项。
